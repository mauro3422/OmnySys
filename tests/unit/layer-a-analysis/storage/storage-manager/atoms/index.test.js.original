import { describe, it, expect } from 'vitest';
import * as atoms from '#layer-a/storage/storage-manager/atoms/index.js';

describe('storage/storage-manager/atoms/index.js', () => {
  it('re-exports atom save/load operations', () => {
    expect(atoms.saveAtom).toBeTypeOf('function');
    expect(atoms.loadAtoms).toBeTypeOf('function');
  });
});

