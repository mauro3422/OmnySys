import { describe, it, expect } from 'vitest';
import { buildAtomMetadata, enrichWithDNA } from '#layer-a/pipeline/phases/atom-extraction/builders/index.js';

describe('pipeline/phases/atom-extraction/builders/index.js', () => {
  it('exports builder helpers', () => {
    expect(buildAtomMetadata).toBeTypeOf('function');
    expect(enrichWithDNA).toBeTypeOf('function');
  });
});
