# v0.9.434 - Sprint 16d: Propagation Drift Reduction and Control-Plane Coverage Repair (2026-04-09)

## Key Achievements

1. Repaired `consolidate_policy_drifts` so the repair path can resolve batch findings again instead of failing with `detectPolicyDriftsBatch is not defined`.
2. Cleared the last `data_gateway` drift by routing folderization snapshot summaries through the canonical data-gateway contract instead of reading raw DB health fields directly.
3. Repaired control-plane propagation adoption so surfaced-system coverage is computed from the full inventory instead of a sampled subset, eliminating false negatives for `health_snapshot`, `rename_folderized_family`, `cache_policy`, and `watcher`.
4. Expanded watcher guard propagation payloads across `async-safety` and `circular-guard`, reducing real `propagation_expansion` backlog while keeping watcher issues aligned with the canonical propagation contract.

## New & Refactored Files

* Refactored: `src/layer-c-memory/mcp/tools/folderization-snapshot-summary.js`
* Refactored: `src/layer-c-memory/mcp/tools/get-health-snapshot.js`
* Refactored: `src/shared/compiler/compiler-explainability-loader.js`
* Refactored: `src/shared/compiler/folderization-automation-utils.js`
* Refactored: `src/shared/compiler/policy-drift-repairer.js`
* Refactored: `src/core/file-watcher/guards/async-safety/collection.js`
* Refactored: `src/core/file-watcher/guards/async-safety/core.js`
* Refactored: `src/core/file-watcher/guards/async-safety/issues.js`
* Refactored: `src/core/file-watcher/guards/async-safety/persistence.js`
* Refactored: `src/core/file-watcher/guards/async-safety/reporting.js`
* Refactored: `src/core/file-watcher/guards/circular-guard/context.js`
* Refactored: `src/core/file-watcher/guards/circular-guard/cycle-reporters.js`
* Refactored: `src/core/file-watcher/guards/circular-guard/detection.js`
* Refactored: `src/core/file-watcher/guards/circular-guard/imports.js`
* Refactored: `src/core/file-watcher/guards/circular-guard/issue-service.js`

## Validation

* `node --check` passed for the repaired MCP tool and propagation/control-plane files touched in this batch.
* `mcp_omnysystem_consolidate_policy_drifts({ policyArea: "data_gateway" })` returned `0` findings after the folderization summary repair.
* `mcp_omnysystem_get_health_panel({ scopePath: "src" })` reported `controlPlane.propagation.state = ready` with `8/8` surfaced systems.
* `mcp_omnysystem_consolidate_policy_drifts({ policyArea: "propagation_expansion", scopePath: "src/core/file-watcher/guards/async-safety" })` returned `0`.
* `mcp_omnysystem_consolidate_policy_drifts({ policyArea: "propagation_expansion", scopePath: "src/core/file-watcher/guards/circular-guard" })` returned `0`.

## Impact

* `data_gateway` drift dropped from `1` to `0`.
* Control-plane propagation adoption moved from an incomplete surfaced set to `8/8` ready coverage.
* `propagation_expansion` backlog dropped from `134` to `125`.
* Remaining propagation work is now concentrated in real watcher clusters such as `complexity-guard`, `conceptual-duplicate-risk`, and `dead-code`, instead of false negatives in the propagation surface itself.

## Known Follow-up

* Propagation drift is reduced but not closed; the next highest-value cluster is `src/core/file-watcher/guards/complexity-guard`.
* Watcher diagnostics still report some local governance debt and duplicate/helper noise in the compiler utilities touched during this batch.
