# v0.9.228

## Fixed
- Canonicalized telemetry accessors in the websocket server, websocket client, and batch processor facade.
- Kept `getStats()` as compatibility aliases while introducing domain-specific stats accessors.
