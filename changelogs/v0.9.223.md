# v0.9.223

- Canonicalized the test-generation recommendation helper with `buildTestRecommendations()` and kept the old export as a compatibility alias.
- Preserved the test tooling contract while reducing generic `generateRecommendations()` surface area.
