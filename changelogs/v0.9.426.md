# v0.9.426

## Propagation Surfaces

- Surfaced `propagation` directly in `status-server-details`, `get-system-inventory-report`, and `get-canonical-promotion-report` so the public control-plane surfaces expose the shared contract instead of burying it in nested snapshot payloads.
- Split `diagnose-tool-health` into analysis/rules/core helpers and moved the canonical promotion report orchestration into a runner helper so the public tool files stay flatter and watcher-friendly.

## Validation

- `node --check` passed for `status-server-details`, `get-system-inventory-report`, `get-canonical-promotion-report`, and the new helper modules.
- `vitest` passed for `diagnose-tool-health`, `check-pipeline-integrity`, and `technical-debt-report` coverage.
- `get_recent_errors()` returned no active watcher warnings after the refactor.
