# v0.9.118

- Split `get_server_status` support code into focused helpers:
  - `status-metadata.js`
  - `status-compiler-explainability.js`
- Hardened `status.js` with explicit degraded-path error handling for:
  - `get_server_status()`
  - `get_recent_errors()`
- Simplified `status-compiler.js` so it only owns deep vitals assembly.
- Updated integrity recommendations so lifecycle/orphan reconciliation is described as system-managed observation, not a manual cleanup action.
- Improved startup dashboard wording:
  - warns when bootstrap metrics are still settling
  - avoids misleading `File Universe` coverage messaging during transient startup states
- Verified canonical runtime metrics remain aligned after full reindex:
  - `fileUniverseSummary`
  - `conceptualActionableRatio`
  - `MCP Sessions`
