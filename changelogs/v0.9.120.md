# v0.9.120

## Debt Reduction And Performance Cleanup

- reduced structural debt across multiple static-analysis and MCP hotspots by splitting monolithic files into thinner coordinators plus focused helper modules
- removed several real performance hotspots by replacing repeated linear scans with sets, maps, cached indexes, and shared traversal helpers
- continued cleaning coordinator bias in watcher/integrity heuristics so builders, analyzers, and resolvers are judged more fairly

## Key Refactors

- modularized `atomic-writer-tool` preflight and duplicate-guard logic into `atomic-write-helpers`
- optimized and decomposed:
  - `CrossFileResolver`
  - `ChainBuilder`
  - `connection-generator`
  - `closure-tracker`
  - race-detector timing and matcher paths
  - `relationship-analyzer`
  - `investigate-graph-opportunities`
  - `export-detector`
  - `import-analyzer`
  - `unused-imports`
- cleaned multiple semantic query and generate-tests surfaces so wrapper noise and duplicate naming pressure were reduced

## Governance And Signal Quality

- added `resolver` to canonical operational-role classification and aligned confidence/integrity heuristics with that role
- removed a policy-level conceptual duplicate by renaming the internal archetype helper in `atom-role-classification`
- lowered policy complexity in `signal-confidence` by extracting coordinator-bias checks into a dedicated helper

## Validation

- imports remained clean on touched hotspots after each refactor pass
- pipeline integrity stayed at `100 / A+`
- pipeline health stayed at `100 / A`
- runtime freshness was kept aligned through repeated MCP restart/reload cycles while editing OmnySys against itself
