# v0.9.321 - Sprint 16: File Watcher Core Helper Rename Batch

## Summary

- Renamed the file-watcher core helpers so internal implementations no longer share the same public names as the watcher facade.
- Kept the public `file-handlers.js` API stable while redirecting it to the renamed internal helpers.
- Reduced conceptual duplicate pressure around `saveAtom`, `getAtomsForFile`, `enrichAtomsWithAncestry`, and `detectCircularDependencyForFile`.

## Validation

- Direct `node --input-type=module` smoke imports passed for `file-handlers-core-helpers`, `file-handlers.js`, and the watcher handler modules.
- The watcher helper exports remained stable after the rename batch.
