# v0.9.357 - Sprint 16: Integrity Guard Data-Flow Split Batch

## Summary
- Split the integrity guard data-flow module into context, skip, violation, and analysis helpers.
- Kept the public integrity guard entrypoint stable while shrinking the monolithic data-flow helper.
- Preserved the existing data-flow semantics and the low-coherence / unused-input checks.

## Validation
- Smoke import passed for the integrity guard data-flow tree and the core integrity guard entrypoint.
- `node --input-type=module` import checks completed successfully for the updated modules.
