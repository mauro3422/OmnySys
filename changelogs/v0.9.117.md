# v0.9.117

## Metrics Runtime And Status Split

- centralized runtime/compiler metric snapshots in `src/shared/compiler/compiler-runtime-metrics.js`
- exposed canonical summaries for:
  - atom inventory
  - graph coverage
  - file universe coverage
  - conceptual duplicate actionable/raw ratio
  - MCP session health and multi-client churn
- wired those summaries into:
  - `get_server_status()`
  - MCP startup/final dashboard output

## Status Tool Decomposition

- split `src/layer-c-memory/mcp/tools/status.js` into focused helpers:
  - `status-runtime.js`
  - `status-notifications.js`
  - `status-compiler.js`
- preserved the external tool contract while reducing the main file size and improving maintainability

## Integrity And Reconciliation

- introduced `src/core/meta-detector/integrity-contract.js` as a shared integrity recommendation contract
- aligned integrity recommendation wording with system-managed reconciliation instead of manual cleanup scripts

## Watcher And Freshness

- normalized hot-reload watcher startup behavior and exposed watcher noise telemetry
- improved runtime freshness visibility for stale tool/runtime modules

## Residual Notes

- watcher still reports some conceptual/policy findings around the new status helper split
- current residual findings are governance-quality issues, not import/runtime breakage
- runtime verification after restart remained healthy and the metrics surfaces stayed coherent
