# v0.9.273

## Changed
- Canonicalized file-watcher analysis logging through a shared helper.
- Kept `collectAndIndexFile()` focused on orchestration and guards.

## Verification
- `node --check src/core/file-watcher/analyze.js`
- `get_recent_errors()` returned zero active issues after reload
