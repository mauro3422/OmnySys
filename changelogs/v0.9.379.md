# v0.9.379 - Sprint 16: Folderization Dependency Rewrite Repair

## Scope
- Folderization move/rewrite reliability for moved families with internal sibling imports and export-from dependencies.

## Changes
- Extended the folderization planner so `export ... from` edges count as real dependencies when scoring barrels and computing migration impact.
- Taught the move/rewrite flow to rewrite imports inside the moved file itself, not only in dependents, so folderized barrels do not keep stale sibling paths after relocation.
- Kept the runtime-metrics family importable after the move by correcting the internal root-vs-folder paths for `pipeline-orphans.js` and `dead-code-reporting.js`.

## Verification
- `node --input-type=module` import smoke passed for `src/shared/compiler/compiler-runtime-metrics/index.js`.
- `node --input-type=module` import smoke passed for `src/shared/compiler/index.js`.
- `node --input-type=module` import smoke passed for `src/shared/compiler/compiler-runtime-metrics/compiler-runtime-metrics-summary.js`.
- `node --check` passed for the touched runtime-metrics modules.
