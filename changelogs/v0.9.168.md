# v0.9.168 - Sprint 16: Data Gateway Freshness Contract

## Summary

- Added a DB-first `data-gateway` contract that classifies compiler surfaces as `fresh`, `partial`, `stale`, `missing`, or `blocked`.
- Exposed generation and primary issue details in the compact compiler status payload so freshness drift is easier to inspect from MCP.
- Added a regression test for the contract to keep the DB-first policy explicit.

## Details

- `src/shared/compiler/data-gateway-contract.js` now acts as the policy ledger for compiler-facing surfaces.
- `src/shared/compiler/compiler-diagnostics-snapshot.js` publishes the contract alongside the canonical analysis generation snapshot.
- `src/layer-c-memory/mcp/tools/status-summary.js` now surfaces the contract's generation and primary issue in compact form.
- `tests/unit/shared/compiler/analysis-generation.test.js` covers the contract behavior against fresh and stale surface inputs.

## Validation

- `node --check` passed on the touched modules.
- `vitest run tests/unit/shared/compiler/analysis-generation.test.js` passed.
- The runtime remains stale until MCP reloads the updated modules.
