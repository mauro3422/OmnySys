# v0.9.425

## Propagation Surfaces

- Split `diagnose-tool-health` into analysis/report helpers so the MCP tool stays flatter and easier for the watcher to reason about.
- Exposed the canonical propagation plan on `diagnose-tool-health` responses so the tool no longer hides the plan inside nested report payloads.
- Surfaced `propagation` directly from `pipeline_health` so the pipeline health handler publishes the shared contract instead of burying it in the governance helper.

## Validation

- `node --check` passed for `diagnose-tool-health`, the new analysis/report helpers, and the pipeline-health handler.
- `vitest` passed for `diagnose-tool-health`, `check-pipeline-integrity`, and `technical-debt-report` coverage.
- `get_recent_errors()` returned no active watcher warnings after the refactor.
