# v0.9.203

## Changed
- Split `analyzeAtomDataFlow` in `src/core/file-watcher/guards/integrity-guard.js` into smaller private helpers.
- Kept the integrity guard behavior stable while lowering branching in the core data-flow check.

## Notes
- This is a maintenance refactor only; no policy or payload shape changed.
