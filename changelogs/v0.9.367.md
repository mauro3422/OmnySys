# v0.9.367 - Sprint 16: Status Tool Inventory Summary Refactor

## Summary
- Moved MCP tool inventory compaction out of `status-summary.js` into a dedicated status helper.
- Kept `get_server_status()` behavior unchanged while reducing manual recomposition in the compact status path.
- Preserved the inventory report added in the prior batch and kept the output shape stable.

## Validation
- Smoke import passed for the new status inventory helper, `status-summary.js`, and `status.js`.
