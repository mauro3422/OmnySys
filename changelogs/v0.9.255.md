# v0.9.255

## Changes

- Added canonical `system_files` snapshot queries and routed file analysis/export reads through the query API instead of ad-hoc SQL.
- Expanded system-map repair so mirrored metadata is rebuilt when `system_files` falls behind the active file universe, not only when the table is empty.
- Updated DB-access guidance to point `system_files` reads to the canonical snapshot API.

## Verification

- `node --check` passed for the updated query, repair and detection modules.
- `vitest` passed for database-health and metadata extraction coverage.
- Live server status returned `0` errors and `0` warnings after the reindex settled, with `systemMapPersistenceCoverage` back to healthy.
