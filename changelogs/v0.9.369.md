# v0.9.369 - Sprint 16: Mutation Batch Guard and Status Summary Finalization

## Summary
- Added a mutation-batch guard so folderization and move operations can suppress hot-reload while a batch is in progress.
- Deferred runtime reload handling during file-move batches to avoid transient import drift while the filesystem and DB are still reconciling.
- Finalized the MCP status summary helper split so the barrel file now re-exports canonical helpers from tracked modules.

## Validation
- Smoke import passed for `mutation-batch.js`, `move-orchestrator.js`, `hot-reload-manager/index.js`, and `folderize-family.js`.
- `git diff --check` passed after the batch guard and status summary helper files were added.
