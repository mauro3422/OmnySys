# v0.9.213

## Changed
- Renamed the batch processor public ready-changes API to `getReadyChangesSnapshot()` to avoid a naming collision with the grouped-change helper.
- Updated watcher change processing to use the new batch processor API name.

## Notes
- Behavior is unchanged; the rename only removes a false recursion signal in the watcher graph.
