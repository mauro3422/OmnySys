# v0.9.197 - Split File Watcher Handlers

**Fecha:** 2026-03-22
**Estado:** Completed

## Summary

Reduced the size of `src/core/file-watcher/handlers/file-handlers.js` by extracting file-created, impact-wave, and duplicate-risk actions into dedicated helper modules.

## Changes

- Added `src/core/file-watcher/handlers/file-handlers-create.js`.
- Added `src/core/file-watcher/handlers/file-handlers-actions.js`.
- Kept the public watcher handler contracts intact.
- Reduced `file-handlers.js` below the governance size threshold.

## Validation

- `node --check` passed on all touched watcher handler modules.
- The MCP runtime needs a reload to pick up the new module split.
