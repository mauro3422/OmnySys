# v0.9.329 - Sprint 16: Async Safety Guard Split

## Summary

- Split `async-safety-guard` into a public barrel plus dedicated modules for async analysis, issue construction, persistence, and orchestration.
- Preserved the public `detectAsyncSafetyIssues` export and the `runtime:async-safety` event emission path.
- Reduced the guard's monolithic surface while keeping the existing test-file skips, production-purpose filters, and rate-limited reporting behavior.

## Validation

- Direct `node --input-type=module` smoke imports passed for `async-safety-guard` and `default-semantic-guard-definitions`.
- The split preserved the watcher-facing export contract.
