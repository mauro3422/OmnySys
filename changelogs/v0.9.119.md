# v0.9.119

## Watcher And Runtime Integrity

- fixed false-positive watcher churn by hardening the main file watcher lifecycle against startup/runtime noise
- aligned runtime freshness with live in-memory state instead of historical restart logs
- suppressed low-signal `sem_data_flow_*` alerts for coordinator-heavy orchestrators with zero real data-flow
- fixed `HashCache` stale-entry deletion so incremental bootstrap no longer logs `paths is not iterable`

## Canonical Surface Adoption

- moved more compiler/runtime consumers onto existing canonical `shared/compiler` surfaces
- normalized persisted structured-field access and scanned-file manifest usage through shared compiler APIs
- improved compiler diagnostics/standardization reporting so canonical adoption is measured from real consumers instead of optimistic defaults
- taught guards/backlog guidance to recommend thin coordinators/orchestrators when monolithic files cross agent-risk thresholds

## Technical Debt Reduction

- split large coordinator files into focused modules while preserving contracts:
  - `pipeline-integrity-detector`
  - `phase2-indexer`
  - `prioritized-backlog-handler`
  - `complexity-guard`
  - `standardization-report`
  - `pipeline-health-handler`
- reduced `impact-wave` cost by filtering low-signal callback churn before expensive post-edit validation
- cleaned `pipeline-health-handler` and related governance surfaces without reopening canonical drift

## Validation

- repeated MCP/runtime verification after each refactor step
- imports remained clean across touched modules
- runtime finished fresh and watcher alerts reconciled to zero for the stabilized surfaces
