# v0.9.416

## Propagation in Archive and Tool Telemetry

1. Surfaced folderization propagation in the compiler health dashboard tool telemetry so the derived plan is visible in the same collection that reports tool noise and cache policy.
2. Preserved folderization propagation in the daily archive summary so historical health snapshots can replay the same propagation plan without recomputing it.
3. Split the compiler metrics snapshot summary builders into a dedicated helper module so the compact snapshot path stays under the watcher complexity and file-size thresholds.
