# v0.9.173 - Sprint 16: Surface Audit Status Propagation

## Summary

- Propagated the canonical `surfaceAudit` block through the compact compiler explainability summary so `get_server_status` no longer collapses it to an empty default.
- Added a regression test to keep the surface audit visible in the compact status payload.
- Kept the DB-first surface audit contract intact while fixing the presentation layer wiring.

## Details

- `src/layer-c-memory/mcp/tools/status-summary.js` now carries `surfaceAudit` through `compactCompilerExplainabilitySummary()` and into the live status summary.
- The compact status path continues to reuse `summarizeSurfaceAuditForStatus()` so the same canonical audit shape is used everywhere.
- `tests/unit/layer-c-memory/mcp/tools/status-summary.test.js` covers the compact explainability payload so the audit surface cannot be dropped again.

## Validation

- `node --check` passed on the touched status module.
- `vitest run tests/unit/layer-c-memory/mcp/tools/status-summary.test.js` passed.
