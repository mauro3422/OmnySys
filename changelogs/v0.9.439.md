# v0.9.439 — Hotspot Refactoring + Semantic Granularity API + Bug Fixes (2026-04-12)

## Bug Fixes

- Fixed `get_technical_debt_report` crash: `buildFolderizationReportFromRepo(...).catch is not a function` — wrapped sync call in `Promise.resolve()` so `.catch()` works in `Promise.all`
- Fixed `persistFolderizationSnapshot` undefined `result` variable bug — added `const result = stmt.run(params)` before assigning `result.archive`

## Canonical Surfaces

- Registered `semantic_surface_contracts` in `CANONICAL_SURFACE_REGISTRY` as canonical surface for semantic summary vs detail comparison
- Exported `buildSemanticGranularityComparison` and `summarizeSemanticGranularity` from `shared/compiler/index.js`
- Integrated `summarizeSemanticGranularity` into `metrics-current/index.js` current snapshot pipeline

## Hotspot Refactoring (5 functions, CC reduced from ~218 to ~34 total)

| Function | CC Before | CC After | File |
|----------|-----------|----------|------|
| `buildTechnicalDebtReportValues` | 64 | ~6 | `technical-debt-report-cache-helpers.js` |
| `buildFolderizationPropagationSummary` | 44 | ~8 | `propagation-summary.js` |
| `persistFolderizationSnapshot` | 43 | ~8 | `folderization-snapshot/index.js` |
| `deriveTransportAlerts` | 35 | ~6 | `compiler-runtime-metrics-sessions-state.js` |
| `buildPromotionSummary` | 32 | ~6 | `canonical-promotion-summary.js` |

### Refactoring approach

- Extracted logical sub-responsibilities into focused helper functions
- Eliminated duplicated code blocks (e.g., propagation mode decision repeated twice in `buildFolderizationPropagationSummary`)
- Replaced long inline parameter lists with builder/payload functions
- Each extracted helper is independently testable and has a single reason to change

## Net impact

- 14 files changed
- -798 +634 lines (net -164 lines removed)
- 5 hotspots below CC threshold of 15
- 2 runtime bugs fixed
- 1 new canonical surface registered
