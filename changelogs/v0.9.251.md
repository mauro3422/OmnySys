# v0.9.251

## Changed
- Canonicalized queue presence APIs and removed the last `has*`/`getAll` naming drift from `AnalysisQueue`.
- Canonicalized `PatternDetectorRegistry` presence and lifecycle methods to reduce generic registry aliases.
- Refined `ModificationTracker` and `TunnelVisionDetector` naming so cleanup and history APIs are more explicit.
- Reduced false-positive watcher noise around `TunnelVisionDetector` and related pattern-detection surfaces.
- Kept the runtime restart boundary in `auto` mode with clean recovery flow after live edits.
