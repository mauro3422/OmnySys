# v0.9.230

## Fixed
- Canonicalized tunnel-vision telemetry accessors across the logger, detector, tracker, and stats helpers.
- Added explicit domain-specific stats names while preserving `getStats()` compatibility aliases.
