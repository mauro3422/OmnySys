# v0.9.413

## Propagation Expansion Drift

1. Added a canonical `propagation_expansion` policy area so watcher and tool surfaces can report when propagation is not being surfaced where expected.
2. Surfaced propagation-expansion drift in the compiler health dashboard, status table, and drift assessment so the live panel shows the recommendation alongside the reason.
3. Expanded the propagation-expansion detector to cover watcher and tool surfaces that emit status, health, snapshot, summary, or report payloads without attaching the shared propagation contract.
