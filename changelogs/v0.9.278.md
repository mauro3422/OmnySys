# v0.9.278

## Changed
- Removed an unused import from the default impact guard definitions.
- Kept the file-watcher guard definitions aligned with the shared lazy metadata helpers.

## Verification
- `node --check src/core/file-watcher/guards/default-impact-guard-definitions.js`
- `get_recent_errors()` returned zero active issues after reload
