# v0.9.415

## Propagation in Health Consumers

1. Surfaced folderization propagation in the compiler health dashboard so the compact health view can read the derived plan directly.
2. Added folderization propagation to the one-line health panel summary so the runtime panel can show the decision without drilling into folderization internals.
3. Split the health dashboard panel test into a dedicated file to keep the watcher-aware test surface under the file-size threshold.
