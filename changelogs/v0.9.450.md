# v0.9.450 - Semantic Guard Propagation Fix & Duplicate Alert Auto-Sync

**Fecha:** 2026-04-16  
**Tipo:** Propagation Convergence + Semantic Guard Fix  
**Impacto:** Alto

---

## Resumen

Esta versión corrige un bug de propagation donde los átomos se actualizaban pero los guards de semantic (incluyendo duplicate detection) no se re-ejecutaban automáticamente, causando alertas desincronizadas.

El problema se manifestó cuando `consolidateConceptualCluster` ejecutaba cambios en múltiples archivos, los átomos se re-indexaban correctamente pero las alertas de duplicates no se actualizaban.

---

## Cambios Principales

### 1. Semantic Guards ahora se ejecutan después de cambios de archivo

**Archivos modificados:**
- `src/core/file-watcher/handlers/file-handlers-modified.js`
- `src/core/file-watcher/handlers/file-handlers-modified-helpers.js`
- `src/core/file-watcher/handlers/file-handlers-create.js`

**Antes:** El watcher solo ejecutaba impact guards después de cambios de archivo.

**Después:** El watcher ejecuta semantic guards (incluyendo duplicate guard) después de:
- Archivos modificados (`handleFileModified`)
- Archivos nuevos (`handleFileCreated`)

```javascript
// Flow ahora converge automáticamente:
Archivo modificado 
  → collectAndIndexFile (re-indexa átomos) 
  → runSemanticGuards (ejecuta duplicate guard)
  → persistWatcherIssue/clearWatcherIssue (alertas sincronizadas)
```

### 2. Propagation Drift Templates Documentados

**Archivo modificado:**
- `src/shared/compiler/policy-drift-templates.js`

Se agregaron templates para el nuevo tipo de drift:

- `semantic_guard_not_rerun_after_change` - Cuando átomos cambian pero guards no se re-ejecutan
- `duplicate_alert_not_cleared_after_fix` - Cuando se consolidan clusters pero alertas no se limpian

### 3. Consistencia de Código

**Archivo modificado:**
- `src/layer-c-memory/mcp/tools/folderize-family-plan-runner.js`

Consolidación de funciones duplicadas del análisis anterior, reduciendo alertas de duplicates en el proyecto.

---

## Resultado

| Métrica | Antes | Después |
|---------|-------|---------|
| Duplicate alerts activas | 6+ (obsoletas) | 1 (actualizada) |
| Propagation converge | No | Sí |
| Drift documentado | No | Sí |

---

## Notas para siguientes versiones

- El watcher reconciliation ya detectaba este tipo de drift mediante `findOutdatedWatcherAlertIds`
- El sistema ahora converge automáticamente sin necesidad de ejecutar `aggregate_metrics --duplicates` manualmente
- Este patrón aplica a cualquier operación que modifique múltiples archivos (folderization, consolidate, atomic_edit)