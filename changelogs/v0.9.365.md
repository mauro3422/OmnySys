# v0.9.365 - Sprint 16: Folderization Rewrite Map, Tool Inventory, and Metadata Cleanup Fixes

## Summary
- Added a final import rewrite pass for folderization moves so moved families and impacted dependents are rewritten from the DB-backed move map after physical relocation.
- Exposed a canonical MCP tool inventory via `mcp_omnysystem_list_tools`, grouped by query, action, and admin categories from the registry source of truth.
- Fixed repository file cleanup so `deleteFile()` now updates the `files` table as intended, allowing stale folderization candidates to be purged from SQLite metadata.
- Extended folderization validation to check multiple moved targets instead of only the barrel file.

## Validation
- Smoke import passed for the folderization rewrite helper, tool inventory helper, updated repository adapter, and admin tool registry.
- A live planner sample now skips already-folderized families after stale `files` rows are cleaned.
