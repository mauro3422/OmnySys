# Changelog v0.9.398 - Database Health Snapshot Repair and Helper Canonicalization

> **Fecha:** 2026-04-03
> **Tipo:** Hardening + debt cleanup

## Database health snapshot repair

- Fixed the database-health summary load order so the live SQLite projections are read before the current snapshot is assembled.
- Restored the health snapshot from zeroed startup output back to live DB-backed values after daemon restarts.
- Kept the MCP server healthy while preserving the canonical database trust signal.

## Helper canonicalization

- Centralized repeated `asNumber` helpers into the shared compiler core utilities.
- Centralized `isRepositoryReady` into the repository bridge utilities and re-exported it for compatibility.
- Split the last telemetry helper hot spots into smaller mappers so the remaining warnings clear after reload.

## Validation

- `get_health_snapshot` now reports live DB trust and coverage again.
- `get_technical_debt_report` returns `debtScore: 0`.
- `get_recent_errors()` is clean after the reload.
