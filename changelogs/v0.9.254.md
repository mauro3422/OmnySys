# v0.9.254

## Changes

- Split `src/core/file-watcher/analyze.js` persistence work into `analyze-persistence.js`.
- Added an explicit async error boundary inside the persistence helper.
- Kept the watcher and parser behavior unchanged while reducing the size of the main analysis path.

## Verification

- `node --check` passed for the updated file-watcher modules.
- `vitest` passed for parser, integrity-analysis, and history-manager suites.
- Live server status returned `0` errors and `0` warnings after the reload settled.
