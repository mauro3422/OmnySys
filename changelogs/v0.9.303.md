# v0.9.303 - Sprint 16: Compiler Persistence Export Repair and Core Split Batch

## Summary

- Restored the public `compiler-persistence.js` export chain after splitting cleanup helpers into a dedicated module.
- Split `system-map-persistence-repair`, `signal-coverage`, `compiler-diagnostics-snapshot`, `metadata-extraction-coverage`, and `architectural-debt-score` into smaller helper modules.
- Split `compiler-contract-layer-helpers` into entrypoint and surface builders to keep the contract layer thin.
- Restored the `calculateChangePriority` alias in `src/core/unified-server/initialization/batch-processor-init.js` so the unified-server bootstrap index resolves cleanly.

## Validation

- Direct `node --input-type=module` import checks passed for the updated compiler modules and their new helpers.
- The `shared/compiler/index.js` barrel imported cleanly after the `compiler-persistence` export repair.
