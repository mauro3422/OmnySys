# v0.9.330 - Sprint 16: Hotspot Guard Split

## Summary

- Split `hotspot-guard` into a public barrel plus modules for repository query, issue building, persistence, and orchestration.
- Preserved the public `detectHotspots` export and the `perf:hotspot` event emission path.
- Reduced the guard's monolithic surface while keeping its hotspot scoring behavior intact.

## Validation

- Direct `node --input-type=module` smoke imports passed for `hotspot-guard` and `default-impact-guard-definitions`.
- The split preserved the watcher-facing export contract.
