# v0.9.375 - Sprint 16: Folderization Barrel Repair and Session Status Quieting

## Summary
- Repaired the folderized `architectural-pattern-detector` tree after the move so its local barrels point at the correct sibling and parent modules instead of the deleted root paths.
- Renamed the transformation extractor entrypoint to `extractor.js` and updated its folder barrel so the move is importable from Node without stale file references.
- Hardened unified duplicate reporting and file creation logging so missing event emitters or missing watcher loggers no longer crash the runtime during folderization churn.
- Quieted transient SQLite-unavailable session reads in the MCP session manager so status reads stop logging avoidable `database connection is not open` noise.

## Validation
- `node --check` passed on the compiler barrels, folderized detector modules, duplicate-reporting helpers, file-handler create path, session manager methods, and the renamed extractor entrypoint.
- Direct Node imports passed for:
  - `src/shared/compiler/index.js`
  - `src/shared/compiler/architectural-pattern-detector/index.js`
  - `src/layer-a-static/extractors/data-flow/visitors/transformation-extractor/index.js`
- The watcher still emitted transient export-validation warnings while reindexing settled, but the runtime imports themselves loaded successfully.
