# v0.9.325 - Sprint 16: Impact Wave Guard Split

## Summary

- Split `impact-wave` into a thin public barrel and focused helper modules for atom change collection, broken import/caller validation, scoring, context building, and persisted issue cleanup.
- Kept the public `detectImpactWave` export stable for downstream file watcher guard registration.
- Reduced the size and coupling of the impact-wave guard without changing its event emission or watcher persistence behavior.

## Validation

- Direct `node --input-type=module` smoke imports passed for `impact-wave` and `default-impact-guard-definitions`.
- The split preserved the public guard entrypoint and its watcher-facing behavior.
