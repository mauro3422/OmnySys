# v0.9.320 - Sprint 16: File Watcher Core Helpers Split Batch

## Summary

- Split `file-handlers-core-helpers` into ancestry, storage, and circular guard modules while keeping the public barrel stable.
- Preserved the exports consumed by `file-handlers.js` and the watcher lifecycle helpers.
- Reduced the size of the core file-watcher helper module without changing its behavior.

## Validation

- Direct `node --input-type=module` smoke imports passed for `file-handlers-core-helpers`, `file-handlers.js`, and the watcher handler modules.
- The public file watcher core helper exports remained stable after the split.
