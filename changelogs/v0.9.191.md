# v0.9.191 - Fix Shared Helper Import Paths

**Fecha:** 2026-03-22
**Estado:** Completed

## Summary

Fixed the relative import paths for the shared `chunkArray` and `escapeRegExp` helpers after consolidating duplicate utilities.

## Changes

- Corrected `call-relations-linkage.js` and `link.js` to import `chunkArray` from `src/shared/utils`.
- Corrected `return-flow.js` and `singleton-rules.js` to import `escapeRegExp` from `src/shared/utils`.
- Kept the shared helper consolidation intact.

## Validation

- `node --check` passed on the touched modules.
- Direct ESM imports for the affected modules resolved successfully.
