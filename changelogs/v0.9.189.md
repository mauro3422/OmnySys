# v0.9.189 - Canonical Data Gateway Conformance

**Fecha:** 2026-03-22
**Estado:** Completed

## Summary

Added an explicit compiler policy for data-gateway bypasses so modules that read canonical support surfaces directly are classified as drift instead of remaining only a recommendation.

## Changes

- Added `detectDataGatewayConformanceFromSource()`.
- Wired the new policy into the compiler policy drift scan.
- Exported the detector through the shared compiler barrel.

## Validation

- `node --check` passed on the touched modules.
- The shared compiler barrel now exports the new detector.
