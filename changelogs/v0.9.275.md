# v0.9.275

## Changed
- Rebalanced `GuardRegistry` back to a single-class implementation with direct guard registration and execution helpers.
- Kept the file-watcher output helper isolated for consistent compile logging.

## Verification
- `node --check` passed on the affected watcher modules
- `get_recent_errors()` returned zero active issues after reload
