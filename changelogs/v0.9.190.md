# v0.9.190 - Canonical Summary Presentation Conformance

**Fecha:** 2026-03-22
**Estado:** Completed

## Summary

Added an explicit compiler policy for summary recomposition so modules outside the canonical status/snapshot layer are flagged when they rebuild status payloads manually.

## Changes

- Added `detectSummaryConformanceFromSource()`.
- Wired the new policy into the compiler policy drift scan.
- Exported the detector through the shared compiler barrel.
- Added the `summary_presentation` canonical family to the standardization report.

## Validation

- `node --check` passed on the touched modules.
- The shared compiler barrel exports the new detector.
