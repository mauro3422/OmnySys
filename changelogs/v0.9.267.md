# v0.9.267

- Split file watcher handler utilities into a dedicated `file-handlers-core-helpers.js`.
- Kept `file-handlers.js` as a thinner facade over ancestry, storage and circular-dependency helpers.
- Preserved runtime behavior while reducing coordination density in the file watcher hot path.
