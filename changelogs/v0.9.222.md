# v0.9.222

- Split worker file processing into focused helpers and renamed the worker-local `processFile` to avoid semantic collision with the molecular extractor.
- Added local async error handling to the worker analysis helper while keeping the batch pipeline contract stable.
