# Changelog OmnySys v0.9.298

## Sprint 16: Orchestrator Runtime Ops Extraction

### Changed

- Extracted `_syncProjectFiles`, `_invalidateFileCache`, `_triggerIncrementalSocietyUpdate`, and `_runPipelineGuard` from `src/core/orchestrator/helpers.js` into `src/core/orchestrator/runtime-ops.js`.
- Registered the extracted runtime ops on `Orchestrator.prototype` through `src/core/orchestrator/index.js`.
- Reduced `src/core/orchestrator/helpers.js` below the watcher file-size threshold without changing the public orchestrator behavior.

### Validation

- `node --check` passed on `src/core/orchestrator/helpers.js`, `src/core/orchestrator/runtime-ops.js`, and `src/core/orchestrator/index.js`.
- `get_recent_errors()` returned `0 warnings / 0 errors` after the split.
- `watcher_alerts` returned `0` active alerts.
