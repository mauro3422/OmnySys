# v0.9.225

- Canonicalized watcher stats access with `getWatcherStats()` while keeping `getStats()` as a compatibility alias.
- Exported the canonical watcher stats helper through the watcher helper barrel for clearer telemetry semantics.
