# v0.9.348 - Sprint 16: Event Leak Core Split

## Summary

- Split `src/core/file-watcher/guards/event-leak-core.js` into collection and reporting helpers.
- Kept the public event leak guard entrypoint stable while moving issue collection and watcher persistence/emission into dedicated modules.

## Validation

- Smoke import passed for `event-leak-core.js`, `event-leak-collection.js`, `event-leak-reporting.js`, `event-leak-issues.js`, and `event-leak-guard.js`.
