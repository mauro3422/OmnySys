# v0.9.310 - Sprint 16: Signal Coverage Aggregations Split Batch

## Summary

- Split `signal-coverage-aggregations` into candidate coverage, row summary, and pipeline findings modules.
- Kept the `signal-coverage` public barrel stable while separating signal coverage concerns by responsibility.
- Preserved the compiler-level exports consumed by `signal-coverage.js` and `shared/compiler/index.js`.

## Validation

- Direct `node --input-type=module` smoke imports passed for `signal-coverage-aggregations`, `signal-coverage`, and `shared/compiler/index.js`.
- The public signal coverage exports remained stable after the split.
