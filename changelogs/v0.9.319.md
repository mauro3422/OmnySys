# v0.9.319 - Sprint 16: File Watcher Guard Definitions Split Batch

## Summary

- Split the file-watcher default guard catalogs into smaller semantic and impact definition groups while keeping the public barrels stable.
- Preserved the exports consumed by `default-guards`, `registry`, and the guard registration flow.
- Reduced the size of the default guard definition files without changing guard behavior.

## Validation

- Direct `node --input-type=module` smoke imports passed for the semantic and impact guard definition barrels, `default-guards`, and `registry`.
- The public guard definition exports remained stable after the split.
