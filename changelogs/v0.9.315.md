# v0.9.315 - Sprint 16: Live Row Utils Split Batch

## Summary

- Split `live-row-utils` into a query-focused module and a drift-detection module while keeping the public barrel stable.
- Preserved the exports consumed by `database-health`, `analysis-change-reconciliation`, `compiler-diagnostics-snapshot-evidence`, and `shared/compiler/index.js`.
- Removed temporary `.bak` backups and the unused `.cline` Kanban config from the working tree.

## Validation

- Direct `node --input-type=module` smoke imports passed for `live-row-utils`, `database-health`, `analysis-change-reconciliation`, `compiler-diagnostics-snapshot-evidence`, and `shared/compiler/index.js`.
- The public live-row helpers remained stable after the split.
