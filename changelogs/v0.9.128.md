# v0.9.128 - File Watcher Memory Leak Fix & Orchestration Triage

## Key Achievements

1. **FileWatcher SQLite Bug Fix**: Fixed a critical bug in `duplicate-conceptual-core.js` where the background DB query failed to filter out `is_dead_code`. This caused the watcher to constantly raise false positive conceptual duplicate warnings for code that had already been deleted or refactored.
2. **Orchestration Debt Analysis**: Completed a full investigation into 45 high-risk orchestration conceptual duplicates (`process:orchestration:core:main` and `execute:orchestration:core:execute`). Concluded that these were structural necessities for CLI commands and independent standalone scripts, validating them as intentional framework patterns rather than technical debt.
3. **DDD Architectural Suggestions Evaluated**: Executed OmnySys `suggest_architecture` heuristics with low confidence thresholds and confirmed 0 domain scatter warnings, assuring the macro-level module cohesion of the project is currently stable.

## Refactored Files

- `src/core/file-watcher/guards/duplicate-conceptual-core.js` (Added dead code filtration logic)
