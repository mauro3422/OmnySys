# v0.9.214

- Split `StatsPool` into explicit module/system accessors.
- Migrated direct `statsPool.getStats(...)` consumers to `getModuleStats(...)`.
- Kept `getStats()` as a compatibility alias for now.
