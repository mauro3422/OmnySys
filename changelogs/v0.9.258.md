# v0.9.258

- Backfilled `system_files.identifier_refs_json` from persisted `atoms._meta_json`, which is where Tree-sitter metadata already lives.
- Split the metadata coverage repairer into smaller modules to clear the size and complexity warnings.
- Canonicalized the test-callback helper reuse so the repairer no longer duplicates the test-file heuristic.
