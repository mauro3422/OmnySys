# v0.9.460 - Manifest Reconciliation, Mutation Settlement Repair, and Graph Drift Cleanup

Date: 2026-04-18

## Summary

This release closes the file-universe drift that was keeping `databaseHealth` degraded and repairs the remaining graph projection mismatch without a full reindex.

## What changed

- Reconciled the persisted scanned-file manifest with the live scanner universe.
- Fixed the folderized `mutation-settlement` helper imports so the new folder structure loads correctly.
- Reindexed only the two pending Phase 2 files instead of forcing a global reindex.
- Cleared the remaining orphan call relations so `call_graph` and `atom_relations` align again.

## Result

- `status`: `0 warnings`, `0 errors`
- `databaseHealth`: `healthy 100/A+`
- `phase2PendingFiles`: `0`
- `scannedFiles`: `2964`
- `activeFiles`: `2964`

## Notes

- The transport provenance surface is still `watchful`, but it is no longer blocking the runtime.
- This release focused on canonical reconciliation, not on adding new surfaces.
