# v0.9.266

- Split `WebSocketManager` startup/bootstrap responsibilities into `websocket-server-helpers.js`.
- Fixed `runListeningBootstrap()` to call `initializeHeartbeatManager()` after the helper rename.
- Kept the WebSocket server compatible while moving the runtime wiring out of the class body.
