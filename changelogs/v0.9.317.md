# v0.9.317 - Sprint 16: Analysis Generation Split Batch

## Summary

- Split `analysis-generation` into count, snapshot, drift, and summary modules while keeping the public barrel stable.
- Preserved the exports consumed by `compiler-diagnostics-snapshot`, `data-gateway-contract`, `surface-audit`, `core/orchestrator/static-insights-helpers`, and `shared/compiler/index.js`.
- Reduced the size of the generation entrypoint without changing the public generation contract.

## Validation

- Direct `node --input-type=module` smoke imports passed for `analysis-generation`, its new submodules, and `shared/compiler/index.js`.
- The public analysis generation exports remained stable after the split.
