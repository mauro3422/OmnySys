# v0.9.122

## MCP Debt Reduction

- split `mcp-http-server` session routing into a dedicated module to remove the last MCP `HIGH` hotspot
- decomposed the MCP tools registry into thin facades plus family-scoped definition/handler catalogs
- kept the MCP registry green with `131` focused unit tests after each registry split

## Watcher Guard Decomposition

- split default watcher guard catalogs into semantic and impact definition modules
- kept `default-guards.js` as a thin coordinator and removed its medium import-concentration hotspot
- preserved watcher cleanliness with repeated MCP `get_recent_errors()` verification

## Validation

- `npx vitest run tests/unit/layer-c/mcp/tools-registry.test.js tests/unit/layer-c/mcp/mcp-setup-step.test.js --config tests/config/vitest.unit.config.js`
- repeated MCP `restart_server(reindexOnly=true)` and `get_recent_errors()`
- repeated MCP `aggregate_metrics(risk)` and `validate_imports` on the refactored MCP registry and watcher guard files
