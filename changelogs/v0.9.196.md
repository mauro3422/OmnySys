# v0.9.196 - Skip Test Factory Export Validation

**Fecha:** 2026-03-22
**Estado:** Completed

## Summary

Adjusted canonical export validation so `tests/factories/**` surfaces are treated as auxiliary test scaffolding and do not emit broken export-chain warnings in the watcher/runtime path.

## Changes

- Added a test-factory skip rule to `src/layer-c-memory/mcp/tools/validate-exports-chain.js`.
- Added the same skip rule to the file-watcher export validation path.
- Preserved validation for production/compiler surfaces.

## Validation

- `node --check` passed on both touched runtime modules.
- The change requires an MCP reload to be reflected in the live watcher.
