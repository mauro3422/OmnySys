# v0.9.363 - Sprint 16: Duplicate-Aware Folderization Guidance

## Summary
- Linked conceptual duplicate clusters to folderization candidates using persisted import/export metadata.
- Added `folderizationHint` to conceptual duplicate debt issues when a flat family already has a clear folder and barrel.
- Expanded the shared compiler folderization API so duplicate remediation can recommend a folder as well as an SSOT.

## Validation
- Smoke import passed for the folderization helpers, duplication calculator, recommendation engine, and shared compiler barrel.
- `node --input-type=module` import checks completed successfully for the updated modules.
