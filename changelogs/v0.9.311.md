# v0.9.311 - Sprint 16: Architectural Debt Score Calculator Split Batch

## Summary

- Split `architectural-debt-score-calculators` into directory, pattern, coupling, and duplication modules.
- Kept the public architectural debt score API stable while moving each scoring concern into its own module.
- Reduced the remaining complexity in the score pipeline without changing the compiler barrel surface.

## Validation

- Direct `node --input-type=module` smoke imports passed for `architectural-debt-score-calculators`, `architectural-debt-score`, and `shared/compiler/index.js`.
- The public architectural debt score exports remained stable after the split.
