# v0.9.372 - Sprint 16: Folderization Metrics in Status and Technical Debt

## Summary
- Exposed folderization family state and migration summary in `compilerExplainability` so the live status payload can show `flat`, `mixed`, and `already_folderized` families.
- Added folderization candidate and family-state sections to the technical debt report, including actionable folderization moves for approved families.
- Kept the duplicate/group guidance aligned so conceptual duplicate clusters can now point at an existing folderized family when one already exists.

## Validation
- Smoke import passed for `status-compiler-explainability.js`, `status-summary-helpers.js`, and `technical-debt-report.js`.
- `git diff --check` passed after the status/debt folderization additions.
