# v0.9.338 - Sprint 16: Topology Regression Guard Split

## Summary

- Split `src/core/file-watcher/guards/topology-regression-guard.js` into signal analysis and reporting helpers.
- Kept the public topology regression guard entrypoint stable while moving topology matching, severity selection, and watcher persistence out of the main file.

## Validation

- Smoke import passed for `topology-regression-guard.js`, `topology-regression-signals.js`, `topology-regression-reporting.js`, and `default-impact-guard-definitions.js`.
