# v0.9.180 - Sprint 16: Framework Tracker Hook Policy

## What changed
- Taught the duplicate-signal policy to treat race-detector `trackMolecule()` hooks as intentional framework contracts.
- Added regression coverage so tracker subclasses do not get flagged as accidental conceptual duplicates.
- Kept non-framework `trackMolecule` surfaces actionable.

## Why
- The race-detector tracker hierarchy uses a Template Method pattern, so repeated `trackMolecule()` implementations are expected framework hooks, not duplicate business logic.
- The policy now classifies that pattern the same way it already classifies MCP `performAction`, initialization `rollback`, and type-contract `calculateConfidence`.

## Validation
- `node --check` passed for the touched policy files.
- `vitest` passed for the duplicate-signal policy regression.
- `get_recent_errors()` stayed clean after the edit.
