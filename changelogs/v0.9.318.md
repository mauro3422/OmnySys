# v0.9.318 - Sprint 16: File Watcher Analyze Utils Split Batch

## Summary

- Split `analyze-utils` into hash, diff, import resolution, and result builders while keeping the public utility barrel stable.
- Preserved the exports consumed by `analyze-flow`, `analyze.js`, and the file watcher handlers.
- Reduced the size of the file-watcher utility module without changing the watcher analysis contract.

## Validation

- Direct `node --input-type=module` smoke imports passed for `analyze-utils`, `analyze-flow`, `analyze.js`, and the new utility submodules.
- The public file watcher utility exports remained stable after the split.
