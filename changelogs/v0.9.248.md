# v0.9.248

- Improved the batch processor scheduler split so the runtime wake-up path no longer forms a direct drain recursion edge.
- Added `Scheduler` to the compiler boundary-shell heuristic so `BatchScheduler` is treated as infrastructure instead of a false-positive high data-flow alert.
- Covered the heuristic change with a unit test for `BatchScheduler`.
