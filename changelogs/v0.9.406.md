# v0.9.406

## Folderization Propagation Visibility

1. Added an explicit folderization `propagation` summary so the snapshot now shows move targets, impacted files, rewrites, validation targets, and the top affected candidates instead of hiding them inside nested migration-plan data.
2. Threaded that propagation summary through the folderization snapshot service, technical-debt report, and compiler explainability compactors so the same signal is visible in the live dashboard, persisted snapshots, and debt output.
3. Expanded the technical-debt action text for folderization candidates so it now reports dependent file impact alongside move and rewrite counts.
4. Added tests that lock the new propagation contract in both the folderization snapshot and the technical debt report.
