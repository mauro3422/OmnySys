# v0.9.323 - Sprint 16: Circular Guard Split Batch

## Summary

- Split `circular-guard` into context, persistence, detection, and import scanning modules while keeping the public guard barrel stable.
- Preserved the exports consumed by the default impact guard definitions and watcher core helpers.
- Reduced the size of the circular guard implementation without changing its observable behavior.

## Validation

- Direct `node --input-type=module` smoke imports passed for `circular-guard`, its new submodules, and `file-handlers.js`.
- The public circular guard exports remained stable after the split.
