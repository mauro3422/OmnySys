# v0.9.420

## Added
- Simplified `get_health_snapshot` and `get_system_inventory_report` into thin wrapper functions so the watcher no longer needs to score large inline response builders.
- Moved canonical promotion compaction into the shared explainability helper layer and reused the canonical score clamp helper instead of keeping a duplicate local implementation.

## Notes
- The canonical promotion signal stays visible in status, dashboard, metrics, archive, and inventory surfaces.
- The recent refactor removes duplicated clamp logic and keeps the surfaced MCP tools smaller and easier to validate.
