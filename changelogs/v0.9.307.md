# v0.9.307 - Sprint 16: Metadata Coverage Report and Semantic Connection Split Batch

## Summary

- Split `metadata-extraction-coverage-report` into table, count, field, summary, and assembly modules while keeping the public report API stable.
- Split `semantic-surface-derivation-connections` into shared-state and event connection derivation modules so the semantic connection logic is easier to maintain.
- Preserved the public exports consumed by `metadata-extraction-coverage` and `semantic-surface-derivation`.

## Validation

- Direct `node --input-type=module` smoke imports passed for `metadata-extraction-coverage-report`, `metadata-extraction-coverage`, `semantic-surface-derivation-connections`, `semantic-surface-derivation`, and `shared/compiler/index.js`.
- The public report and derivation exports remained stable after the split.
