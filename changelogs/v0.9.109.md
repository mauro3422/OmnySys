# v0.9.109

## Enfoque
Esta versión se concentró en estabilizar el compilador interno, reducir drift entre superficies, endurecer contratos canónicos y bajar ruido falso del watcher para que la deuda reportada se acerque más a la deuda real.

## Mejoras principales
- Se reforzó la capa de contratos del compilador para distinguir mejor entre superficies `canonical`, `advisory`, `compat` y `legacy`.
- Se consolidaron varias reglas y helpers canónicos para evitar que módulos MCP, guards y reporting reimplementen la misma lógica.
- Se redujo ruido en los detectores de duplicados conceptuales/estructurales, especialmente en helpers de bajo valor semántico, wrappers triviales y superficies de compatibilidad.
- Se mejoró la detección de drift de imports/export nombrados para evitar fallos de runtime por barrels incompletos.
- Se estabilizó la reconciliación de issues del watcher, incluyendo limpieza de alerts stale y mejor clasificación de findings de política.

## Cambios técnicos
- Canonicalización de superficies semánticas:
  - `atom_relations` consolidado como fuente de verdad.
  - `semantic_connections` mantenido como superficie `advisory/summary`.
  - Creación de wrappers de compatibilidad para legacy logs (`pattern-analyzer.js`, `stats-manager.js`).
- Mejoras en health/status:
  - Exposición más explícita de canonicidad y contratos del compilador (`compiler-conformance-scan.js`).
  - Snapshots diagnósticos más consistentes entre `status` y `pipeline_health`.
- Duplicate governance:
  - Nueva política para distinguir duplicación real de helpers canónicos, compat shims y ruido de detector (`duplicate-conceptual-core.js`, `duplicate-signal-policy.js`).
  - Mejor clasificación de wrappers locales que bypassan superficies canónicas mediante evaluación de *semantic fingerprints* e ignorando `legacy`.
- Validate imports:
  - Detección de `missing named exports` además de imports rotos.
- Runtime/watcher:
  - Mejor limpieza de alerts stale y reconciliación de watcher issues.
  - Reducción de falsos positivos en guards de circularidad e integridad para ciertos boundary modules.
- Storage/repository:
  - Extracción de helpers canónicos para filtros, policy de fields, persistencia y utilidades de consulta (`atom-schema.js`, `storage-predicate.js`, `dependency-handler.js`).
- Tunnel vision logger:
  - Consolidación de stats/analyzer sobre la superficie canónica existente, marcando legacy tunnel vision analytics con políticas strictas de compatibilidad (`LEGACY_TUNNEL_VISION_COMPATIBILITY_FILE_MARKERS`).

## Correcciones
- Se corrigieron varios casos donde el sistema mezclaba summary surfaces con detail surfaces y generaba contradicciones en métricas.
- Se corrigieron fallos por imports válidos a archivos existentes pero con exports faltantes.
- Se redujeron alerts falsos en detección de duplicados y circularidad.
- Se repararon desalineaciones entre manifest, live rows y surfaces derivadas en varios puntos del pipeline.
- Se resolvió un error recursivo de resolución de módulos (`ERR_MODULE_NOT_FOUND`) que paralizaba el File Watcher y el demonio MCP al cargar los Governance Guards.
- Refactorización de `loadDuplicateGroups` y queries del `dashboard-reporter` para usar utilidades canónicas de `duplicate-dna.js`, bajando las penalizaciones de deuda técnica por queries en crudo.

## Resultado esperado
- Menos contradicciones entre tools MCP.
- Mejor separación entre deuda real y ruido del detector.
- Menor riesgo de que nuevas IAs creen wrappers o mini-superficies paralelas sin advertencia.
- Mayor confiabilidad estructural del sistema para seguir iterando sobre deuda técnica real.

## Nota
Esta versión prioriza estabilidad, gobierno del compilador y calidad diagnóstica por encima de features nuevas.
