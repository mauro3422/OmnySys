# Changelog v0.9.402 - Health Snapshot Explainability Exposure

> **Fecha:** 2026-04-03
> **Tipo:** Observability exposure

## Explainability visibility

- Exposed `policySummary` and `standardization` in the health snapshot tool so the live dashboard can surface the remaining compiler explainability debt instead of hiding it behind a narrower projection.
- Kept the health snapshot contract backward-compatible for the existing database health, folderization, data gateway, contract layer, surface audit, and drift fields.

## Validation

- The source loader and snapshot service already return the policy and standardization data.
- `get_health_snapshot` now projects those fields in the tool response shape.
