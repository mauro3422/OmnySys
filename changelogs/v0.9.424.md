# v0.9.424

## Propagation Surfaces

- Surfaced the canonical propagation plan in `diagnose-tool-health` and `check-pipeline-integrity` so tool-health and integrity reports now carry the same plan language as folderization, status, and metrics consumers.
- Added a top-level propagation summary to the technical debt report so the debt surface no longer hides the plan inside folderization alone.
- Split the technical-debt report load path into a dedicated helper to keep the core loader flatter and reduce watcher complexity pressure.

## Validation

- `node --check` passed for the updated MCP tools and the new technical-debt helper.
- `vitest` passed for `diagnose-tool-health`, `check-pipeline-integrity`, and `technical-debt-report` coverage.
