# v0.9.364 - Sprint 16: Folderization Migration Planner and Approved Family Moves

## Summary
- Added a DB-driven folderization migration planner that returns `approve/review/reject` decisions, move targets, and import rewrite impact.
- Taught the folderization detector to ignore families that are already living inside their own folder.
- Folderized two approved families in the live tree:
  - `pipeline-orphan`
  - `metadata-extraction-coverage`
- Updated the compiler entrypoints so the moved families still import and validate cleanly.

## Validation
- Smoke import passed for the folderization planner, moved families, and shared compiler barrel.
- `getMetadataExtractionCoverage(repo.db)` executed successfully after the folder move.
