# v0.9.294 - Sprint 16: Real Project Factory Lifecycle Rename (2026-03-26)

## Key Achievements

1. Renamed the internal lifecycle methods in [tests/factories/real/project.factory.js](/c:/Dev/OmnySystem/tests/factories/real/project.factory.js) from `create`/`init` to `fromTemplate`/`initialize`.
2. Kept the external helper API stable through `createTestProject()` and `withProject()`.
3. Reduced the create/init/create naming chain that had been showing up in factory recursion diagnostics.

## New & Refactored Files

* Refactored: `tests/factories/real/project.factory.js`

## Validation

* `node --check tests/factories/real/project.factory.js` passed.
* `get_recent_errors()` remained clean after the rename.

## Impact

* Clearer method naming inside the real project factory.
* Lower chance of misleading recursion-style diagnostics on the test factory path.
