# v0.9.246

## Changed
- Relaxed file-watcher integrity/circular heuristics so infrastructure leaf atoms no longer surface as false-positive high-severity data-flow or cycle alerts.
- Canonicalized cache invalidator path normalization with a domain-specific helper to remove generic `normalizePath` noise.
- Kept the cache invalidator aligned with the SQLite-backed cache manager while preserving rollback behavior.

## Verification
- `node --check` passed on the modified watcher and cache invalidator modules.
- `npx vitest run tests/cache-invalidator.test.js --reporter=dot` passed.
- Live watcher alerts were reduced to `0` after the update.
