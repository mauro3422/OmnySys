# v0.9.257

- Expanded metadata extraction repair to backfill `system_files.culture`, `culture_role`, `definitions_json`, `calls_json`, `semantic_analysis_json`, and transitive dependency fields when source data exists.
- Made `getMetadataExtractionCoverage()` auto-repair repairable missing fields before returning the coverage report.
- Kept the runtime stable while the repair ran; `runtimeCodeFresh=true` and `databaseHealth=A+` after reindex.
