# v0.9.226

## Fixed
- Canonicalized batch model stats access with `getBatchStats()` and kept `getStats()` as a compatibility alias.
- Corrected the malformed closing brace in `src/core/batch-processor/models/batch.js`.
