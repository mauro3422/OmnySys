# v0.9.384 - Sprint 16: Repository Bridge Diagnostics Split and Session-Stable Worker Settlement

## Changed

- Split repository retry helpers and diagnostics out of `repository-bridge.js` to bring the bridge back under the file-size threshold.
- Kept Phase 2 persistence serialized in the parent process while removing the `runRepositoryMutation` wrapper from the post-worker settlement path.
- Preserved transient retry semantics for repository mutations while keeping non-transient failures visible.

## Verification

- `node --check` passed for the touched repository and pipeline files.
- `vitest run tests/unit/layer-c-memory/storage/repository/repository-bridge.test.js` passed.
