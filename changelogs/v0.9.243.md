# v0.9.243

## Summary
- Renamed `ErrorClassifier.classify()` to `classifyError()` and updated `ErrorGuardian` to use the canonical method.
- Renamed `ErrorClassifier.addPattern()` to `registerPattern()` and updated the guardian entrypoint accordingly.

## Notable changes
- `src/core/error-guardian/handlers/error-classifier/classifiers/base-classifier.js`
- `src/core/error-guardian/guardian/index.js`

## Notes
- The live runtime stayed fresh after the edit.
- The conceptual duplicate count on `ErrorClassifier` was reduced, but the remaining high semantic warning is architectural and still under review.
