# v0.9.210

## Changed
- Removed `shouldIgnore` from the public `src/core/file-watcher/helpers.js` barrel.
- Kept `shouldIgnore` private to `helpers-surface.js` to avoid a structural duplicate in the watcher helper surface.

## Notes
- The watcher behavior is unchanged; only the public helper surface was narrowed.
