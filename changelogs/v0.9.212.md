# v0.9.212

## Changed
- Renamed internal batch-processor helper to avoid a false recursion signal with the public `getReadyChanges()` method.
- Renamed the shared stats getter factory's inner function to avoid duplicate `getStats` naming in the telemetry stack.

## Notes
- Behavior is unchanged; this only removes naming collisions that were triggering watcher noise.
