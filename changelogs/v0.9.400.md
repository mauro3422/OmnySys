# Changelog v0.9.400 - Compiler Explainability Summary Split

> **Fecha:** 2026-04-03
> **Tipo:** Structural cleanup

## Compiler explainability split

- Split the canonical compiler explainability summary into a shared helper module plus a thin summary wrapper.
- Centralized the compact policy, standardization, contract, drift, metadata, surface audit, folderization, and watcher projections in one reusable helper surface.
- Reduced the explainability summary module to a narrow composition layer so future policy and contract adjustments are easier to reason about.

## Validation

- `src/shared/compiler/compiler-explainability-summary.js` now compiles as a thin wrapper.
- `get_server_status()` remains clean with zero recent errors after the refactor.
