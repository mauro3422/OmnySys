# v0.9.124

## Policy Gap Closure And Duplicate Debt Reduction

- removed the visible `async_error` and `state_ownership` policy gaps by adding explicit async error boundaries in watcher guard definitions and import-validation flows
- aligned watcher/integrity behavior so coordinator-style validators and duplicate lifecycle cleanup no longer produce the same false positives seen during debt reduction work
- fixed duplicate-query filtering so removed atoms are excluded from live duplicate radar even when legacy `is_removed` state lags behind

## Canonical Consolidation Follow-through

- consolidated small structural duplicates into canonical shared helpers for risk levels, score clamping, regex escaping, reason-frequency reporting, node command resolution, and cross-file duplicate checks
- migrated remaining output/transformation AST helper drift onto the shared Tree-sitter path and removed the practical Babel/ESTree residue from the affected extractor helpers
- reduced conceptual duplicate noise by renaming misleading validator responsibilities and improving conceptual-cluster consolidation behavior

## Runtime And Validation Hardening

- hardened `consolidate-conceptual-cluster` so safe cases prefer re-exports or alias wrappers and unsafe method-level consolidations stop with explicit errors instead of generating recursive wrappers
- kept MCP/runtime imports clean after the consolidations and refreshed the runtime to validate the new guard/policy behavior
- verified policy drift cleanup with direct `scanCompilerPolicyDrift(...)` checks for `async_error` and `state_ownership`

## Validation

- MCP `validate_imports` on touched watcher/tool files
- MCP `get_recent_errors`
- MCP `get_server_status`
- MCP `restart_server({ clearCacheOnly: true })`
- direct policy drift scan through `src/shared/compiler/policy-conformance.js`
