# v0.9.155 - Sprint 15: Call Relation Helpers and Surface Noise Reduction

**Date:** 2026-03-20

## Summary

This release trims the remaining medium-severity watcher noise in the call-relation path by splitting the batch persistence helpers and simplifying the shared relation writer. The only medium conceptual duplicate left is the public `saveMany` contract, which is intentionally retained for repository compatibility.

## Key Changes

1.  **Batch Relation Split**: `saveRelationsBatch` now delegates grouping, cache setup, and row insertion to module-level helpers.
2.  **Call Linker Simplification**: `persistCallRelations` now uses a dedicated helper for bulk-vs-fallback row persistence.
3.  **Duplicate Surface Reduced**: medium-severity complexity alerts dropped from the relation path; the remaining `saveMany` warning is a contract-level duplicate, not a data-integrity issue.

## Validation

- Syntax checks passed for `call-relations-linkage.js` and `helpers/relations.js`.
- Runtime data health remains `A+` with aligned call graph and semantic projections.
- Remaining watcher output is low-risk structural noise around contract compatibility and nearby test factories.

