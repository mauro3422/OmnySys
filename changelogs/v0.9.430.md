# v0.9.430 - Policy coverage helper reuse

## Summary

- Centralized policy coverage display resolution in the status helpers.
- Kept the `Aduana` row as a presentation gate over the canonical `policyCoverage` contract.
- Removed the repeated fallback chains from the control-plane table and aligned the tests with the shared helper.

## Validation

- `status-system-table.test.js`
- `status-summary-payload.test.js`
- `compiler-health-dashboard.test.js`
- `get_recent_errors()` returned clean after the refactor
