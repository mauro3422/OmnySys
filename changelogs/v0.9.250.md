# v0.9.250

## Changed
- Canonicalized `generate-tests` recommendation helpers across coverage, smart-test, and schema tools.
- Consolidated pattern-detection metadata onto canonical `description` fields and reduced duplicate `describePattern` accessors.
- Renamed initialization pipeline step gating from `shouldExecute` to `canExecute` and centralized the primary-only guard in the base step.
- Improved compiler integrity heuristics so `Step` classes are treated as boundary shells instead of false-positive data-flow hotspots.
- Renamed pattern-detection score helpers to more specific names and reduced generic `calculateScore` duplication.

