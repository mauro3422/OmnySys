# v0.9.237

## Summary
- Consolidated the duplicated `log()` helper in the MCP stdio bridge into `stdio-bridge-lifecycle.js` and reused it from the transport wiring module.
- This removes the last logic duplicate group in the project metrics.

## Notable changes
- `src/layer-c-memory/mcp-stdio-bridge.js`
- `src/layer-c-memory/mcp/stdio-bridge-lifecycle.js`

## Notes
- Live watcher alerts remain at zero.
- Duplicate groups now read as zero in the MCP duplicate metrics.
