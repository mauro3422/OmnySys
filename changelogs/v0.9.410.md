# v0.9.410

## Impact-Wave Propagation

1. Connected the `impact-wave` file-guard flow to the shared propagation engine so the guard now derives a cacheable plan alongside its alert evidence.
2. Persisted the propagation summary inside the watcher issue context and surfaced it in the emitted `arch:impact-wave` event.
3. Added impact-wave propagation coverage to the shared propagation engine, including change-type defaults and watcher-connected systems.
