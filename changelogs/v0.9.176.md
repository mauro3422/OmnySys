# v0.9.176 - Sprint 16: Metadata Extraction Coverage Aggregation Fix

## Summary

- Fixed the metadata extraction coverage helper so populated metadata fields are aggregated correctly instead of reading a single arbitrary row.
- Added conditional field eligibility so `atoms.deprecated_reason` is only counted when deprecated atoms actually exist.
- The canonical surface audit now reports real coverage for `atoms.called_by_json`, and the primary issue moves to the next genuinely missing field.

## Details

- `src/shared/compiler/metadata-extraction-coverage-helpers.js` now sums populated rows per field with `SUM(CASE ...)` instead of selecting a non-aggregated expression.
- `src/shared/compiler/metadata-extraction-coverage.js` now treats `deprecated_reason` as a conditional metadata field, so it no longer pollutes the primary issue when no atoms are deprecated.
- `metadata-extraction-coverage` now reflects the real DB population for `called_by_json`, and the surface audit no longer treats it as a false zero.
- Added regression coverage to keep `called_by_json` visible as a populated field in the canonical metadata coverage summary.

## Validation

- `node --check` passed on the touched compiler helper files.
- `vitest` passed on the metadata coverage regression and the bulk relation regression.
