# v0.9.380 - Sprint 16: Runtime Metrics Folder Rename Cleanup

## Scope
- Internal naming cleanup for the folderized runtime metrics family.

## Changes
- Renamed the folder-internal modules to short role names: `graph.js`, `issues.js`, and `summary.js`.
- Updated the compiler support barrel so it points at `compiler-runtime-metrics/issues.js` instead of the long legacy basename.
- Kept the public runtime metrics barrel stable while shrinking the internal family layout.

## Verification
- `node --input-type=module` import smoke passed for `src/shared/compiler/compiler-runtime-metrics/index.js`.
- `node --input-type=module` import smoke passed for `src/shared/compiler/compiler-runtime-metrics-support.js`.
- `node --input-type=module` import smoke passed for `src/shared/compiler/index.js`.
- `git diff --check` was clean apart from pre-existing CRLF warnings in other untouched files.
