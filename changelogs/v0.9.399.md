# Changelog v0.9.399 - Technical Debt Cache Slimming and Async Boundary Canonicalization

> **Fecha:** 2026-04-03
> **Tipo:** Hardening + observability cleanup

## Technical debt cache slimming

- Split the technical-debt cache persistence path into smaller helpers so the hot write path stays below the watcher threshold.
- Canonicalized snapshot-path normalization into a shared compiler helper to remove duplicate path handling across folderization and debt reporting surfaces.
- Kept the technical-debt report cache aligned with the canonical compiler snapshot data and preserved fingerprinted persistence.

## Async boundary cleanup

- Introduced a shared async boundary helper and taught the conformance layer to recognize it as a valid async wrapper.
- Wrapped the noisy file-watcher and MCP bridge persistence/reporting paths with explicit async boundaries so observation-only flows stop counting as async-error noise.
- Reduced the remaining runtime observability warning set without changing the live database or session behavior.

## Validation

- `get_recent_errors()` is clean after the reload.
- The technical-debt cache hot path no longer carries the previous large-function warning.
- The live MCP server stays healthy while the shared compiler surfaces absorb the canonical helper changes.
