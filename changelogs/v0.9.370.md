# v0.9.370 - Sprint 16: Folderization Evolution and Existing Family Hints

## Summary
- Added historical metadata signals from `atom_versions` and `files.updated_at` to folderization scoring.
- Suppressed duplicate folderization candidates when a family already exists in a dedicated folder.
- Added existing-folder hints for conceptual duplicate clusters so the system can point to the canonical family location instead of proposing a new folder.

## Validation
- Smoke import passed for the folderization data, analysis, migration, recommendation, and duplication scoring modules.
- `git diff --check` passed after the metadata evolution and existing-family heuristics were added.
