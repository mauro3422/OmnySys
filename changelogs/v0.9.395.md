# Changelog v0.9.395 - Canonical Status Summary Refactor & Debt Cleanup

> **Fecha:** 2026-04-02
> **Tipo:** Hardening + observability

## Status surface refactor

### Canonical assembly
- Moved the full status payload assembly into a canonical shared/compiler explainability helper so the MCP layer can stay a thin adapter.
- Re-exported the metrics snapshot and explainability summaries as direct aliases in the MCP layer to avoid local recomposition.
- Kept the structured status payload shape intact while simplifying the wrapper/import topology.

### Panel layout
- Preserved the terminal control-plane order:
  - Daemon
  - Database
  - Snapshots
  - Update
  - Behavior
  - Drift
  - Debt
  - Sessions
  - Tools
  - Cache
  - Watcher
  - Errors
- Kept `Update` and `Errors` visible in both the runtime status panel and bootstrap summary.

## Debt cleanup

- Reduced the remaining status-surface policy drift by moving metadata extraction, data-gateway, and surface-audit summaries to canonical shared/compiler entrypoints.
- Collapsed the runtime status wrappers so the watcher no longer sees a local summary recomposition path in the main payload assembly.
- Left the repository integrity and update-surface hardening from the previous release intact.

## Validation

- `status-summary-payload` and `status-system-table` tests pass.
- `get_recent_errors()` is clean after the refactor.
- The live status surface still reports the system as healthy and ready.
