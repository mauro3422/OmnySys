# v0.9.249

## Changed
- Canonicalized `BatchProcessor` change ingestion to `queueChange()`.
- Canonicalized the file-watcher batch processor ingestion to `enqueueChange()`.
- Updated unified-server and orchestrator consumers to use the new batch queue APIs.
- Removed the last live conceptual duplicate warning between the two batch-queue surfaces.
