# v0.9.236

## Summary
- Removed the generic `getStats()` alias from `TunnelVisionDetector` and exposed `getTunnelVisionDetectorStats()` as the canonical module accessor.
- Renamed the history cleanup helper to `pruneExpiredHistory()` to stop it from colliding conceptually with the tracker cleanup path.
- Widened the integrity boundary heuristic to treat `Detector` shells as boundary containers, which clears the false positive on `TunnelVisionDetector`.

## Notable changes
- `src/core/tunnel-vision-detector/index.js`
- `src/core/__tests__/tunnel-vision-detector.test.js`
- `src/shared/compiler/integrity-analysis.js`
- `tests/unit/shared/compiler/integrity-analysis.test.js`

## Notes
- The live watcher is back to zero alerts after the heuristic update.
- `runtimeRestartMode` remains `auto` and the daemon is still healthy.
