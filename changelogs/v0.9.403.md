# Changelog v0.9.403 - Compiler Explainability Debt Reduction

> **Fecha:** 2026-04-03
> **Tipo:** Policy cleanup + observability hardening

## Policy cleanup

- Removed the remaining compiler policy drift findings by routing testability, shared-state hotspot, semantic-coverage, and semantic-purity heuristics through the canonical shared compiler layers.
- Reworked the conceptual duplicate finder to use canonical testability evaluation instead of a manual severity heuristic.
- Promoted legacy system-map and shared-state reporting paths to consult canonical coverage/reporting helpers before exposing their results.

## Observability hardening

- Exposed the compiler explainability policy and standardization projections in the health snapshot, so the live dashboard now shows the remaining debt surface directly.
- Kept the health snapshot and dashboard responses backward-compatible while reducing the explainability noise floor to zero findings in the local scan.

## Validation

- `scanCompilerPolicyDrift()` returns `0` findings locally.
- `get_health_snapshot()` now reports `policySummary.total = 0`, `standardization` fully canonical, and `compilerContractLayer.parallelCanonicalSurfaceFindings = 0`.
- `get_recent_errors()` remains clean after the refresh.
