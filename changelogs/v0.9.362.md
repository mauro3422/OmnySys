# v0.9.362 - Sprint 16: DB-Driven Folderization Candidate Scoring

## Summary
- Upgraded folderization detection to use persisted `files.imports_json` and `files.exports_json` metadata.
- Added DB-driven candidate scoring with barrel selection, internal import density, and confidence ranking.
- Exposed the folderization helpers through the shared compiler barrel and fed the report into the architectural debt score.

## Validation
- Smoke import passed for the folderization helpers, architectural debt score, recommendation engine, and shared compiler barrel.
- `node --input-type=module` import checks completed successfully for the updated modules.
