# v0.9.211

## Changed
- Added `createStatsGetter()` to the shared stats pool utility.
- Migrated watcher and batch-processor stats snapshots to the canonical getter factory.

## Notes
- This reduces duplicated telemetry boilerplate without changing emitted stats shapes.
