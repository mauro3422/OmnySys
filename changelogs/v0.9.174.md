# v0.9.174 - Sprint 16: Surface Audit Status Envelope Wiring

## Summary

- Routed the raw canonical `surfaceAudit` from compiler explainability into the live server status envelope so the compact summary can always emit the real audit payload.
- Kept the status presentation layer aligned with the DB-first surface audit contract instead of depending on the compact explainability object alone.

## Details

- `src/layer-c-memory/mcp/tools/status.js` now stores the raw explainability surface audit on the top-level status envelope before compaction.
- The existing `status-summary.js` path can now resolve the audit from either the top-level status envelope or the compact explainability summary without falling back to an empty default.

## Validation

- `node --check` passed on `src/layer-c-memory/mcp/tools/status.js`.
- The live MCP runtime was reloaded after the change so the updated status envelope could be observed.
