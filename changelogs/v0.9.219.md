# v0.9.219

- Canonicalized verification recommendation generation with `buildVerificationRecommendations()` and kept the old export as a compatibility alias.
- Updated the verification report generator to consume the canonical helper.
