# v0.9.263

## Changed

- Split unified duplicate guard persistence and event emission into a dedicated helper.
- Fixed the conceptual duplicate guard flow to pass the project root explicitly.
- Reduced `unified-duplicate-guard.js` below the file-size warning threshold.
