# v0.9.166 - Sprint 16: Dead-Code Barrel Compatibility Fix

**Date:** 2026-03-21

## Summary

This hotfix restores the `isSuspiciousDeadCodeAtom` export on the canonical dead-code compatibility barrel so `shared/compiler/index.js` can import the full dead-code surface again without crashing the MCP bootstrap.

## Key Changes

1. **Barrel Repair**: `dead-code-utils.js` now re-exports `isSuspiciousDeadCodeAtom` from the new dead-code suspicion module.
2. **Bootstrap Recovery**: the compiler index import chain no longer fails when the MCP loads the shared compiler barrel.

## Validation

- `node --check` passes for the dead-code barrel and the compiler index.
- The dead-code helper split remains intact.
- The runtime should start again once the MCP reloads the updated barrel.
