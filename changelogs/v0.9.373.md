# v0.9.373 - Sprint 16: Folderized Naming Normalization and Rename Guidance

## Summary
- Added a DB-driven folderized naming report that shortens verbose member names inside already folderized families while keeping `index.js` as the barrel.
- Normalized already folderized families so the live status, technical debt report, and architectural score can surface naming debt alongside folderization debt.
- Renamed folderized members in `pipeline-orphan`, `metadata-extraction-coverage`, `batch-processor`, `data-loader`, and `issue-manager` to shorter role-based basenames.

## Validation
- Smoke import passed for the new naming report, the updated folderization barrel, the renamed folderized files, and the updated status/technical debt modules.
- `git diff --check` passed after the rename batch.
