# v0.9.253

## Fixes

- Corrected the parser logger import path so `src/layer-a-static/parser/index.js` resolves `src/utils/logger.js` again.
- Expanded integrity boundary heuristics to treat `Parser` shells as infrastructure containers.
- Split the `HistoryManager` unit coverage into a core spec and an extended spec to reduce file-size noise.

## Verification

- `node --check` passed for the touched runtime and test files.
- `vitest` passed for parser, integrity-analysis, and history-manager suites.
- Live MCP status returned zero active errors after the restart.
