# v0.9.299 - Sprint 16: Atom Extractor Complexity Split and Cache Invalidator Run Helper

## Summary

- Split `src/layer-a-static/pipeline/phases/atom-extraction/extraction/atom-extractor.js` into smaller helpers to reduce the main extraction path complexity.
- Centralized SQL parent lookup into a dedicated helper and kept `linkSqlAtoms` as a thin coordinator.
- Extracted cache invalidation execution in `src/core/cache/invalidator/index-methods.js` into a shared `runInvalidation` helper.

## Validation

- `node --check` passed for the touched runtime files.
- `get_recent_errors()` returned `0 warnings / 0 errors` after reindex.
