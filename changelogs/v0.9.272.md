# v0.9.272

- Collapsed the remaining `WebSocketManager` method wrappers into single-line prototype assignments.
- Reduced the facade size enough to clear the medium watcher alert on the websocket server.
