# v0.9.360 - Sprint 16: Folderization Candidate Detection Batch

## Summary
- Added a folderization candidate detector for flat helper families in the compiler.
- Integrated folderization candidates into the architectural directory score and recommendation engine.
- Exposed the detector through the shared compiler barrel for reuse.

## Validation
- Smoke import passed for the folderization detector, architectural debt score, and shared compiler barrel.
- `node --input-type=module` import checks completed successfully for the updated modules.
