# v0.9.175 - Sprint 16: Surface Audit Reason Normalization

## Summary

- Normalized surface audit reasons so object-shaped summaries no longer collapse into `[object Object]` in the canonical data-gateway contract.
- Kept the DB-first audit surface readable for the live status summary and explainability views.

## Details

- `src/shared/compiler/data-gateway-contract.js` now resolves surface reasons from structured summary fields before falling back to string coercion.
- `surface-audit` now carries readable reasons through the compact status envelope for the metadata extraction coverage surface.

## Validation

- `node --check` passed on the touched compiler surface files.
- `vitest` passed on the surface audit regression coverage.
