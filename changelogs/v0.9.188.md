# v0.9.188 - Restore Batch Processor Compatibility

**Fecha:** 2026-03-22
**Estado:** Completed

## Summary

Restored the public `SmartBatchProcessor` batch read APIs after the control-flow split so the file watcher can keep using the canonical processor contract.

## Changes

- Reintroduced `getCurrentWindow()` and `getReadyChanges()` as compatibility wrappers.
- Kept the extracted helpers for internal batch orchestration.
- Removed the duplicate comment introduced during the refactor.

## Validation

- `node --check` passed on `batch-processor.js`.
- The watcher path now has the same public surface it expected before the refactor.
