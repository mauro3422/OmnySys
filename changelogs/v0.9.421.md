# v0.9.421

## Added
- Added canonical startup regression telemetry so bootstrap summaries can distinguish an expected full reindex startup from a true startup regression.
- Surfaced the startup signal in the server status table, compiler health dashboard, health snapshot, and bootstrap dashboard summaries.

## Notes
- The new startup summary is derived from the initialization pipeline, Layer A strategy, and the total bootstrap duration.
- Full reindex bootstraps are now classified as expected-slow instead of being mistaken for generic startup regressions.
