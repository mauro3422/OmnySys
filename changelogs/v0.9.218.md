# v0.9.218

- Centralized detector identity in `PatternDetector` and removed redundant `getId()` overrides from pattern detectors.
- Kept detector runtime behavior stable while reducing conceptual duplication in the pattern detection layer.
