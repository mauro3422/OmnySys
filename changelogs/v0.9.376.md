# v0.9.376 - Sprint 16: Session Persistence Transparency in Runtime Status

## Summary
- Made the MCP session summary explicitly report its persistence mode so the status surface can distinguish SQLite-backed reads from memory fallback without reintroducing noisy transient errors.
- Added a session-manager persistence-state probe that reports `available`, `mode`, `source`, and `reason` so callers can tell whether the status is reading live SQLite data or reconstructed cache state.

## Validation
- `node --check` passed on the updated session-manager methods and compiler session metrics helper.
- `get_server_status()` returned a clean runtime after the restart window, with repository health still reporting `healthy` and no active watcher alerts.
