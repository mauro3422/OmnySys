# v0.9.279

## Changed
- Added a canonical async error boundary around file-watcher handler entrypoints.
- Removed an unused handler logger import and kept handler orchestration centralized.

## Verification
- `node --check src/core/file-watcher/handlers/file-handlers.js`
- `get_recent_errors()` returned zero active issues after reload
