# v0.9.177 - Sprint 16: Atom Metadata Surface Backfill

## What changed
- Backfilled `atoms.exports_json` from canonical export flags.
- Backfilled `atoms.imports_json` from the owning file import surface.
- Backfilled `atoms.side_effects_json` from existing data-flow metadata.
- Added canonical JSON surfaces in the atom metadata builder so future extractions persist them directly.
- Added a regression covering the new atom metadata surfaces.

## Why
- The metadata audit was still pointing at legacy empty atom surfaces even though the DB had enough canonical signals to reconstruct them.
- This release aligns the atom metadata surfaces with the DB-first contract and keeps the summary focused on real remaining gaps.

## Validation
- `node --check` passed for the touched files.
- `vitest` passed for the pipeline group regression.
- Runtime health is clean; the remaining stale module warning is only the reload-needed metadata builder entry.
