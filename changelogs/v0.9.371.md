# v0.9.371 - Sprint 16: Folderization Family State Reporting

## Summary
- Added explicit family state reporting for folderization candidates: `flat`, `mixed`, and `already_folderized`.
- Exposed family evolution metadata in the architectural debt score so the report can show how mature a family is.
- Kept the duplicate-folderization guard aligned with the existing family registry so the system avoids proposing folders that already exist.

## Validation
- Smoke import passed for the folderization analysis, migration, and architectural debt score modules.
- `git diff --check` passed after the family state reporting update.
