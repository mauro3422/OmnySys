# v0.9.179 - Sprint 16: Database Health Orchestration Split and Orphan Reconciliation

## What changed
- Split `database-health.js` into a thin orchestrator plus dedicated count, live-row sync, report, and assessment helpers.
- Reconciled stale `calls` orphan relations from the live graph projection so the canonical health surface stays aligned.
- Kept the health summary DB-first while reducing the public orchestration surface below the governance threshold.

## Why
- The health path was doing too much work in one place and was harder to keep fresh across live projections.
- The orphan relation cleanup was a real projection drift, not a cosmetic stale flag.

## Validation
- `node --check` passed for the touched health helpers.
- `get_server_status()` reports `databaseHealth = A+` and `orphanCallRelations = 0`.
- `get_recent_errors()` is clean.
