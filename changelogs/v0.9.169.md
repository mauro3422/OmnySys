# v0.9.169 - Sprint 16: Active Files Parity Alignment

## Summary

- Updated `metadataSurfaceParity` to compare mirrored `system_files` coverage against the active file universe instead of the full historical file table.
- Added a regression test to keep the parity contract tied to active rows.

## Details

- `src/shared/compiler/metadata-surface-parity.js` now treats active files as the mirrored baseline, which matches how `system_files` is persisted.
- `tests/unit/shared/compiler/analysis-generation.test.js` now covers the active-files parity rule so it cannot regress back to the historical-total heuristic.

## Validation

- `node --check` passed on the touched modules.
- `vitest run tests/unit/shared/compiler/analysis-generation.test.js` passed.
- The runtime still needs a reload for the live MCP to pick up the parity heuristic change.
