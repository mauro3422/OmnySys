# v0.9.358 - Sprint 16: Circular Guard Detection Split Batch

## Summary
- Split the circular guard detection flow into file-graph and atom-cycle helpers.
- Kept the `detectCircularDependencies(...)` entrypoint stable while shrinking the main detection module.
- Preserved the existing cycle classification and persistence behavior.

## Validation
- Smoke import passed for the circular guard detection tree and persistence module.
- `node --input-type=module` import checks completed successfully for the updated modules.
