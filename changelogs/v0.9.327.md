# v0.9.327 - Sprint 16: Event Leak Guard Split

## Summary

- Split `event-leak-guard` into a public barrel and dedicated modules for listener analysis, issue building, persistence sync, and orchestration.
- Kept the public `detectEventLeaks` export stable while removing the monolithic implementation from the entrypoint.
- Preserved watcher event emission and severity handling for runtime event leak detection.

## Validation

- Direct `node --input-type=module` smoke imports passed for `event-leak-guard` and `default-semantic-guard-definitions`.
- The split preserved the watcher-facing export and the semantic guard registration path.
