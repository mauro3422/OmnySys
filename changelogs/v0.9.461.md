# v0.9.461 - Folderization Transaction Canonicalization and Watcher Cleanup

Date: 2026-04-19

## Summary

This release centralizes the folderization transaction contract into the canonical compiler layer and clears the last active watcher alert around folderization execution.

## What changed

- Moved the folderization transaction orchestration into `src/shared/compiler/folderization-transaction.js`.
- Turned `folderize-family-plan-runner/execution.js` into a thin canonical adapter.
- Routed `folderize-family.js` through the compiler standardization report instead of ad-hoc shared-state hotspot logic.
- Kept compiler policy / circular-risk helpers on canonical barrel imports.
- Cleared the last active watcher alert for the folderization runner after the contract refactor.

## Result

- Technical debt report now has `0` active watcher alerts.
- `issuePersistence.activeIssueCount` is `0`.
- The folderization pipeline is still available through the existing MCP surface, but the contract is now centralized instead of split across legacy helpers.

## Notes

- `get_server_status()` still has a separate `normalizeState is not defined` issue in the live MCP wrapper surface.
- That status-tool bug is unrelated to this release and should be handled separately.
