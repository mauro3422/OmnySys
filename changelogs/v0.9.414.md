# v0.9.414

## Propagation in Consumers

1. Elevated folderization propagation into the compact compiler metrics snapshot so the derived plan is visible outside the folderization family.
2. Surfaced propagation explicitly in the structured status payload so downstream consumers can read the plan without drilling into folderization internals.
3. Kept the propagation contract reusable by preserving cache keys, decision mode, and connected systems in the compact snapshot path.
