# v0.9.121

## MCP Runtime And Watcher Canonicalization

- reduced false-positive MCP health noise by separating legitimate multi-client activity from actionable duplicate-session churn
- clarified graph-coverage reporting so call-link totals and high-level semantic links are no longer treated as equivalent surfaces
- fixed stale watcher duplicate alerts by reconciling against canonical `atoms` and current duplicate-policy decisions instead of local heuristics alone

## Layer A And Benchmark Baselines

- lowered complexity in `async-error-conformance` and kept the medium-complexity alert closed
- added the initial non-JS/TS language contract surface for Layer A adapter onboarding
- added minimum benchmark coverage for MCP startup, incremental reindex, and post-edit guards
- added a Layer A language-adapter contract test

## MCP Tooling Hardening

- fixed `atomic_edit` runtime validation regressions and split pre-edit orchestration into focused helpers
- corrected circular-import reporting so only direct-import evidence can produce file-cycle findings
- decomposed compiler runtime metrics into focused graph/session/support modules
- kept watcher reconciliation logic thin by extracting duplicate-policy checks into a dedicated helper

## Validation

- `npx vitest run tests/contracts/layer-a-language-adapter.contract.test.js --config tests/config/vitest.unit.config.js`
- `npx vitest run tests/performance/mcp-startup.benchmark.test.js tests/performance/mcp-incremental-reindex.benchmark.test.js tests/performance/mcp-post-edit-guards.benchmark.test.js`
- `npx vitest run tests/unit/layer-c/mcp/tools-registry.test.js tests/unit/layer-c/mcp/mcp-setup-step.test.js --config tests/config/vitest.unit.config.js`
- repeated MCP `restart_server(clearCacheOnly=true)` plus `get_recent_errors()` verification; remaining watcher noise is only `arch_impact_low` on the new duplicate-policy helper
