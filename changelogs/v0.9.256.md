# v0.9.256

## Changed
- Canonicalized `testCallbackType` and `usesJson` on atom metadata so the extractor persists direct test callback and usage coverage.
- Persisted `files.hash` consistently across the batch, worker, and single-file writers.
- Extended file-summary persistence to keep the hash column in sync with analysis output.
- Exposed the new atom fields in the metadata registry and cache invalidation map.

## Verification
- `node --check` passed on the touched runtime files.
- `vitest` passed for the pipeline metadata builder and file summary storage tests.
- The live runtime should refresh automatically through hot reload; reindex/restart only if the daemon still reports stale coverage after the watcher settles.
