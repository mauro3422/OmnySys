# v0.9.432 - Sprint 16b: Restart Process Split and Canonical Folderization Signals (2026-04-09)

## Key Achievements

1. Split the process-restart lifecycle out of `src/layer-c-memory/mcp/restart-runtime.js` into `restart-runtime-process.js`, keeping the router under the watcher size threshold and preserving the true process restart ACK/recovery contract.
2. Reused the canonical compiler drift and semantic-surface contracts inside the folderization snapshot summary so live-row and semantic-surface guidance now flows from the shared source of truth instead of parallel heuristics.
3. Hardened bridge telemetry persistence so the derived runtime state is written back to disk, preventing stale `booting` snapshots after reconnect/restart cycles.
4. Added regression coverage for the process-restart handler and kept the restart/folderization path stable under the full validation suite.

## New & Refactored Files

* Added: `src/layer-c-memory/mcp/restart-runtime-process.js`
* Refactored: `src/layer-c-memory/mcp/restart-runtime.js`
* Refactored: `src/layer-c-memory/mcp/tools/folderization-snapshot-summary.js`
* Refactored: `src/layer-c-memory/mcp/stdio-bridge-telemetry.js`
* Refactored: `tests/unit/layer-c-memory/mcp/restart-runtime.test.js`

## Validation

* `npm run check` passed after the restart split and canonical summary wiring.
* Targeted Vitest coverage passed for restart, folderization snapshot service, folderization automation summary, and bridge telemetry paths.

## Impact

* Restart handling is easier to edit safely and now avoids carrying process-restart orchestration inside the main router.
* Folderization guidance now reads from canonical drift and semantic contracts, reducing duplicated heuristics.
* Bridge runtime telemetry now reflects the derived settled state instead of lingering in stale booting snapshots.
