# v0.9.207

## Changed
- Removed the duplicate `reloadMetadata` / `queueInitialAnalysis` wrapper API from `src/core/unified-server/initialization/index.js`.
- Updated `src/layer-c-memory/mcp/restart-runtime.js` to call the canonical `analysis-manager.reloadMetadata` entrypoint directly.

## Notes
- This removes a conceptual duplicate without changing the underlying analysis behavior.
