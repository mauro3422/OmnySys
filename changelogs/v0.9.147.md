# v0.9.147 - Sprint 15: Live-Row Hotfix & Helper Hygiene

**Date:** 2026-03-19

## Summary

This hotfix closes a runtime regression introduced while splitting the live-row reconciliation helpers and removes a dead import from the canonical live-row path.

## Key Changes

1.  **Live-Row Runtime Fix**: Restored the `toCount` dependency used by `live-row-utils` so canonical live-row counts resolve at runtime again.
2.  **Helper Hygiene**: Removed an unused import from `live-row-utils` to keep the module lean after the refactor.
3.  **Release Hygiene**: Bumped the package version and updated the release index so the hotfix is tracked as a discrete release.

## Notes

- The runtime still prefers canonical DB surfaces over JSON fallbacks.
- `live-row-utils` remains a refactor candidate because of its complexity footprint, but the helper split itself is now stable.
