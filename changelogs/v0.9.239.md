# v0.9.239

## Summary
- Removed the generic `getStats()` alias from `src/core/cache/manager/stats.js` and kept `getCacheManagerStats()` as the canonical accessor.
- Updated `getAllStats()` to call the canonical cache manager accessor directly.

## Notable changes
- `src/core/cache/manager/stats.js`

## Notes
- Live runtime stayed clean after the edit.
- Duplicate metrics remain at zero.
