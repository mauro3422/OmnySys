# v0.9.178 - Sprint 16: Watcher Provenance and Live Database Health Auto-Sync

## What changed
- Added watcher provenance and surface counts to the canonical status summary.
- Split watcher compaction into a dedicated helper so the status surface stays under the governance threshold.
- Made canonical database health auto-sync live-row projections before scoring.
- Split the database-health scorer into `database-health-assessment.js` to keep the public summary below the governance threshold.
- Reconciled orphan `calls` relations in `atom_relations` so the canonical call graph and health summary line up again.
- Kept the live status surfaces wired to the same DB-first freshness contract.

## Why
- The status surfaces were still reporting an older call-graph projection even though the DB could reconcile it canonically.
- This release makes the health path self-healing for supported live-row drift while keeping the DB as the source of truth.

## Validation
- `node --check` passed for the touched compiler and pipeline-health modules.
- `get_server_status()` now reports watcher provenance and `databaseHealth = A+`.
- `get_server_status()` now reports `databaseHealth = A+` and `orphanCallRelations = 0`.
- `surfaceAudit` is trustworthy again after the live-row reconciliation.
