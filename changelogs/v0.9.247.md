# v0.9.247

## Changed
- Renamed the file-watcher analysis entrypoints from generic `analyze*` names to `collect*` names to reduce conceptual duplicate noise in the core analysis path.
- Kept the file-watcher analysis flow intact while updating the two direct callers to the new canonical exports.
- Preserved the existing cache invalidator behavior and kept the watcher runtime clean after the rename.

## Verification
- `node --check` passed on the modified watcher modules.
- `npx vitest run tests/unit/layer-c-memory/mcp/runtime-change-policy.test.js tests/cache-invalidator.test.js --reporter=dot` passed.
- `get_recent_errors()` returned zero active watcher alerts after the update.
