# v0.9.252

## Changed
- Expanded live-row reconciliation to remove orphaned `atom_relations` rows across relation types instead of only `calls`.
- Renamed the relation cleanup helper to reflect its broader canonical scope.
- Cleared the remaining `orphaned_data` critical issue in live integrity checks.
