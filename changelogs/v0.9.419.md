# v0.9.419

## Added
- Canonical promotion surfaced in the raw compiler health dashboard daily payload so the live dashboard stays aligned with archive and snapshot consumers.
- Compiler health dashboard fixture extracted into a dedicated test helper so the main dashboard test stays under the watcher file-size threshold.

## Notes
- The canonical promotion signal now travels through status, dashboard, snapshot, archive, and system inventory consumers.
- The dashboard test contract stays split to keep the watcher from flagging the main health dashboard test file.
