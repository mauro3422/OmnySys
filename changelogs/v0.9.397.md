# Changelog v0.9.397 - Policy Drift Cleanup and Telemetry Canonicalization

> **Fecha:** 2026-04-03
> **Tipo:** Hardening + observability

## Policy drift cleanup

- Moved status summary modules back onto canonical barrels.
- Removed manual summary recomposition paths from the status layer.
- Reduced live `policy_drift` findings from the status-related surfaces.

## Telemetry canonicalization

- Restored `tool-run-telemetry` as a single canonical implementation with a shared summary helper.
- Tightened the repair classifier so observation-only tools no longer count as repair thrash.
- Added a short TTL cache to avoid repeated telemetry summary recomputation in tight loops.

## Validation

- `tool-run-telemetry`, `status-system-table`, and `status-summary-payload` tests pass.
- `get_recent_errors()` is clean after the refactor.
- Live status remains healthy and ready, with `policy_drift` reduced to 43 medium findings.
