# v0.9.332 - Sprint 16: Dead Code Guard Split

## Summary

- Split `dead-code-guard` into a public barrel plus focused modules for atom evaluation, issue construction, persistence, and orchestration.
- Preserved the public `detectDeadCode` export and the `code:dead-code` event emission path.
- Kept the dead-code remediation and severity behavior intact while reducing monolithic guard logic.

## Validation

- Direct `node --input-type=module` smoke imports passed for `dead-code-guard` and `default-semantic-guard-definitions`.
- The split preserved the watcher-facing export contract.
