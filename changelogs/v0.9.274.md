# v0.9.274

## Changed
- Canonicalized guard definition metadata construction for file-watcher semantic and impact guards.
- Reduced repeated `domain/version/description` boilerplate via shared guard metadata helpers.

## Verification
- `node --check` passed for the guard definition modules
- `get_recent_errors()` returned zero active issues after reload
