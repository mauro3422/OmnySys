# v0.9.312 - Sprint 16: Watcher Issue Storage Runtime Split Batch

## Summary

- Split `watcher-issue-storage-runtime` into snapshot, symbol-check, and runtime-dependency helpers.
- Kept the public watcher storage runtime API stable while reducing the orchestration file size.
- Preserved the public `findOutdatedWatcherAlertIds` entrypoint consumed by `watcher-issue-storage.js` and the compiler barrel.

## Validation

- Direct `node --input-type=module` smoke imports passed for `watcher-issue-storage-runtime`, `watcher-issue-storage`, and `shared/compiler/index.js`.
- The public watcher issue storage runtime exports remained stable after the split.
