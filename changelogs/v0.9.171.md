# v0.9.171 - Sprint 16: Atom-Semantic Surface Repair

## Summary

- Rebuilt `semantic_connections` and `system_files.semantic_connections_json` from atom semantic metadata so the semantic summary surface is DB-first and no longer stuck at zero when atoms already carry semantic telemetry.
- Aligned semantic governance, reuse guidance, and contract-layer messaging with atoms as the canonical semantic source of truth.
- Restored full database health after repairing the semantic surface and cleaning orphan call relations.

## Details

- `src/shared/compiler/system-map-persistence-repair.js` now derives semantic connections from atom semantic metadata and persists them back into both `semantic_connections` and `system_files.semantic_connections_json`.
- `src/core/diagnostics/runtime-table-health-helpers.js` now repairs semantic surface drift before surfacing the semantic warning, so the live health path can self-heal instead of only reporting the gap.
- `src/shared/compiler/semantic-surface-granularity.js`, `src/shared/compiler/database-health.js`, `src/shared/compiler/data-gateway-contract.js`, `src/shared/compiler/compiler-contract-layer.js`, `src/shared/compiler/compiler-contract-layer-helpers.js`, `src/shared/compiler/canonical-reuse-guidance.js`, and `src/shared/compiler/standardization-report/recommendations.js` now describe semantic telemetry as atom-backed, not atom_relations-backed.
- The live database was reconciled after the code update, restoring `semantic_connections` rows and clearing orphan call relations.

## Validation

- `node --check` passed on all touched compiler, policy, and diagnostics modules.
- A direct DB repair pass repopulated `semantic_connections` with 73 rows and `system_files.semantic_connections_json` with 14 files carrying semantic summaries.
- `getDatabaseHealthSummary()` and `buildRuntimeHealthIssues()` now return `A+` / healthy on the repaired database.
- The live MCP runtime still needs a reload to pick up the newly patched repair path and guidance strings.
