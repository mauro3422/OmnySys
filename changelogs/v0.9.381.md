# v0.9.381 - Sprint 16: Canonical Mutation Settlement and Duplicate Conceptual Folderization

## Scope
- Close mutation batches with a canonical settlement step instead of treating move/folderize/rename as success immediately after the filesystem move.
- Fold the conceptual duplicate guard family into a folderized barrel and keep the duplicate-risk paths aligned with the new surface.

## Changes
- Added `src/layer-c-memory/mcp/core/shared/mutation-settlement.js` with a shared mutation snapshot plus retry/reindex validation flow.
- Wired the settlement step into `move-orchestrator`, `folderize-family`, `rename-folderized-family`, `atomic-editor-tool`, and `atomic-writer-tool` so post-mutation validation runs through the MCP tooling path.
- Redirected conceptual duplicate guard imports to the new `src/core/file-watcher/guards/duplicate-conceptual/index.js` barrel and removed the legacy flat duplicate-conceptual modules.
- Simplified folderized rename impact collection by reusing shared dependency-target helpers and the canonical compiler index barrel for folderization path normalization.

## Verification
- `node --check` passed for the touched MCP and file-watcher modules.
- `vitest run tests/unit/layer-c-memory/mcp/core/mutation-settlement.test.js --config tests/config/vitest.unit.config.js` passed.
- `get_recent_errors` was clean after the reindex/settlement pass.
