# v0.9.291 - Sprint 16: Invalidator File Status Rename (2026-03-26)

## Key Achievements

1. Renamed the invalidator status accessor in [src/core/cache/invalidator/index.js](/c:/Dev/OmnySystem/src/core/cache/invalidator/index.js) to a file-specific name so it no longer collides conceptually with orchestrator status helpers.
2. Updated [tests/cache-invalidator.test.js](/c:/Dev/OmnySystem/tests/cache-invalidator.test.js) to exercise the new canonical method name.
3. Kept the behavior unchanged: the invalidator still normalizes the input path before delegating to the validator.

## New & Refactored Files

* Refactored: `src/core/cache/invalidator/index.js`
* Refactored: `tests/cache-invalidator.test.js`

## Validation

* The invalidator method remains a pure read path over the validator.
* The rename is narrow and does not change the status payload shape.

## Impact

* Removes one watcher-visible conceptual duplicate from the cache invalidator surface.
* Improves naming specificity for the invalidator API.
