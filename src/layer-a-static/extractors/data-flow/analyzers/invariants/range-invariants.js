/**
 * @fileoverview Range Invariants Detector
 *
 * Temporary no-op implementation to keep analyzer contracts stable while
 * the full invariant engine is being migrated.
 */

export function detectRangeInvariants() {
  return [];
}

