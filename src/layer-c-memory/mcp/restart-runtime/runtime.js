/**
 * @fileoverview Re-export for backwards compatibility.
 * Points to the canonical handler inside the folderized restart-runtime/ directory.
 */

export { handleRuntimeRestart } from './handler.js';

