// LLM health checker desactivado — LLM no se usa en esta versión.
// Los insights se derivan estáticamente desde átomos en _deriveStaticInsights().

export function _startLLMHealthChecker() {
  // No-op: LLM desactivado
}
